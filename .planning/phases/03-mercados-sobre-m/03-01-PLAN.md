---
phase: 03-mercados-sobre-m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-contrast.cjs
  - .planning/phases/03-mercados-sobre-m/03-CONTRAST-RESULTS.md
  - src/data/size-data.ts
autonomous: true
requirements: [REQ-markets-twelve]
must_haves:
  truths:
    - "scripts/check-contrast.cjs accepts an optional --markets flag (or runs all by default) and reports the same 5-pair contract for each of the 12 market themes"
    - "Every market that previously failed WCAG AA is remediated with the smallest possible token tweak (or surfaced to the user as a pending blocker if remediation requires palette change)"
    - "After remediation, pnpm check:contrast exits 0 for M baseline AND all 12 markets"
    - ".planning/phases/03-mercados-sobre-m/03-CONTRAST-RESULTS.md captures the before/after ratios per market with a one-line note per remediation"
  artifacts:
    - path: "scripts/check-contrast.cjs"
      provides: "Extended to cover the M baseline + 12 market themes from src/data/size-data.ts"
      contains: "SIZE_MARKETS"
    - path: ".planning/phases/03-mercados-sobre-m/03-CONTRAST-RESULTS.md"
      provides: "Per-market contrast audit with before/after if any token was tuned"
      contains: "WCAG AA"
  key_links:
    - from: "pnpm check:contrast"
      to: "12 market theme blocks"
      via: "script imports SIZE_MARKETS via dynamic require + transpiles TS via a minimal regex extractor (or compiles the data file with esbuild already in pnpm)"
      pattern: "for (const m of SIZE_MARKETS)"
---

<objective>
Extend the WCAG AA contrast check to cover all 12 market themes. Surface and remediate any market that fails. Phase 3 cannot be marked done until every market clears WCAG AA on the same 5-pair contract used for M.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-mercados-sobre-m/03-CONTEXT.md
@scripts/check-contrast.cjs
@src/data/size-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check-contrast.cjs to all 12 markets</name>
  <files>scripts/check-contrast.cjs</files>
  <read_first>
    - scripts/check-contrast.cjs (current — only checks M baseline)
    - src/data/size-data.ts (SIZE_MARKETS — 12 entries with theme: { primary, secondary, bg, ink, ... })
    - .planning/phases/03-mercados-sobre-m/03-CONTEXT.md (D-01: smallest possible token tweak; D-02 not in this plan)
  </read_first>
  <action>
    Extend `scripts/check-contrast.cjs`:

    1. Add a market token table extracted from `src/data/size-data.ts`. Since the script is pure Node and the data file is TypeScript, parse the file as text and extract the SIZE_MARKETS array with a regex that captures `id`, `theme.primary`, `theme.secondary`, `theme.bg`, `theme.ink` per entry. Validate that exactly 12 entries are extracted; abort with a clear message otherwise.

    2. Reuse the existing M-pair logic in a `checkPairs(label, tokens)` function. For each market, build a token bag where:
       - `bg = theme.bg`
       - `ink = theme.ink`
       - `accent = theme.primary` (the dot, the bright-cta, the meta line use this)
       - `accent2 = theme.secondary`
       - `muted = color-mix(in srgb, ink 55%, transparent over bg)` — the App.vue applies the `html.has-market` rule that derives muted from ink. Compute the effective muted via the existing alpha-blend helper.
       - `line_strong = color-mix(in srgb, ink 28%, transparent over bg)` (informational, not in required pairs).

    3. Run the same 5-pair contract per market (body INK on BG ≥ 4.5; muted on BG ≥ 4.5; large heading INK on BG ≥ 3.0; accent CTA BG-on-ACCENT ≥ 4.5; accent inline ACCENT on BG ≥ 3.0) and print one block per market with the same dashed-rule formatting used for M.

    4. After all 13 blocks (M + 12 markets), print a final "OVERALL" line:
       - all-pass: `OVERALL  13/13 themes pass WCAG AA at the required thresholds`
       - any-fail: `OVERALL  X/13 themes pass — failing: cpg(2), tech(1), …` where the parenthetical is the count of failing pairs in that theme. Exit non-zero.

    5. Add a `--markets-only` CLI flag that skips the M block and only prints market blocks (used by humans when triaging market fails). Default behavior (no flag) prints M then markets.

    6. Document in code comments: WHY the muted derivation uses 55% and not the raw `--muted: #8a8a85` from the M block (because in `html.has-market` the global rule kicks in and overrides muted).

    Do NOT introduce any third-party dependency. Use only Node built-ins (fs, path).

    Surface — do NOT fix — any failing markets. Remediation is Task 2.
  </action>
  <verify>
    <automated>node scripts/check-contrast.cjs 2>&1 | tee /tmp/contrast.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/contrast.txt','utf8'); const blocks=t.match(/[A-Z][^\\n]{0,40}\\(\\w+\\)\\s+\\d+\\.\\d+:1/g)||[]; if(blocks.length<13*5){console.error('expected ≥65 pair lines (13 themes × 5 pairs), got',blocks.length);process.exit(1)} console.log('block count ok:',blocks.length);"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-contrast.cjs` parses 12 markets from src/data/size-data.ts (script aborts with clear error if it gets ≠12)
    - Each market prints a 5-line pair block matching M's format
    - Final OVERALL line names the count of passing themes (e.g. "9/13") and lists failing markets when any fail
    - `--markets-only` flag skips the M block
    - No new dependencies in package.json
  </acceptance_criteria>
  <done>The script gives a complete, re-runnable picture of WCAG AA across all 13 themes (M + 12 markets).</done>
</task>

<task type="auto">
  <name>Task 2: Triage failures and remediate with minimum-tweak edits</name>
  <files>src/data/size-data.ts, .planning/phases/03-mercados-sobre-m/03-CONTRAST-RESULTS.md</files>
  <read_first>
    - Output of `pnpm check:contrast` from Task 1
    - src/data/size-data.ts (SIZE_MARKETS — the prototype palettes)
    - .planning/phases/03-mercados-sobre-m/03-CONTEXT.md (D-01: smallest tweak; never replace palette)
    - ExistingData/prototype-extracted/00020878_*.js (only if you need to confirm a prototype hex value matches what's in size-data.ts)
  </read_first>
  <action>
    For each market that failed Task 1's contract:

    1. Identify which pair failed (e.g. accent CTA = bg-on-primary < 4.5).
    2. Determine the smallest possible token change that clears the threshold:
       - For body/muted/heading fails: usually darken the bg or lighten the ink by a small step (e.g. 5–10 lightness units in HSL space).
       - For accent CTA fails: deepen the primary so bg-on-it has more contrast, OR lighten the bg behind it. Prefer changing the primary because it preserves the bg/ink relationship for body copy.
       - For accent inline fails: same — deepen or lighten the accent so accent-on-bg has ≥3.0.
    3. Apply the change to the SIZE_MARKETS entry's `theme` block in src/data/size-data.ts.
    4. Re-run `pnpm check:contrast`. If still failing, adjust until the threshold is met.
    5. If a single market needs more than 1 token change to clear all thresholds, STOP and surface to the user — that's a palette-rework, not a tweak.

    Document each market's audit in `.planning/phases/03-mercados-sobre-m/03-CONTRAST-RESULTS.md`:

    ```markdown
    # Phase 3 — Per-market WCAG AA contrast audit

    Generated: <date>
    Source: pnpm check:contrast against src/data/size-data.ts SIZE_MARKETS

    ## M baseline
    body 18.16:1, muted 5.71:1, large 18.16:1, accent CTA 5.56:1, accent inline 5.56:1 — PASS

    ## Markets

    | id | body | muted | large | cta | inline | verdict | tweak |
    |----|-----:|------:|------:|----:|-------:|---------|-------|
    | cpg | 12.34:1 | 5.10:1 | 12.34:1 | 4.51:1 | 4.51:1 | PASS | — |
    | banca | … | … | … | … | … | PASS | — |
    | …
    ```

    Append a "Tweaks applied" section listing each (market, token, before, after, reason).

    Final state: `pnpm check:contrast` exits 0; `03-CONTRAST-RESULTS.md` reports 13/13 PASS; the verdict column in the table is all PASS.

    If you cannot get a market to pass with ≤1 token tweak, write the row as PARTIAL with the failing pair + the proposed multi-token rework + abort the plan; the user will sign off the rework before you commit.
  </action>
  <verify>
    <automated>pnpm check:contrast 2>&1 | tee /tmp/contrast.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/contrast.txt','utf8'); if(!/OVERALL\\s+13\\/13 themes pass/.test(t)){console.error('not all themes pass:\\n',t);process.exit(1)} console.log('all 13 pass');"; node -e "const fs=require('fs'); const p='.planning/phases/03-mercados-sobre-m/03-CONTRAST-RESULTS.md'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); for(const m of ['cpg','banca','retail','automotriz','salud','bebidas','inmobiliario','educacion','turismo','tech','moda','startups']){if(!s.includes(m)){console.error('missing market in audit:',m);process.exit(1)}} console.log('audit covers 12 markets');"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:contrast` exits 0 with `OVERALL 13/13 themes pass`
    - `03-CONTRAST-RESULTS.md` exists and lists all 12 market ids
    - All applied tweaks are documented with before/after
    - No market required >1 token change without user sign-off (auto-aborts otherwise)
    - `pnpm type-check` and `pnpm build` still pass after token edits
  </acceptance_criteria>
  <done>WCAG AA verified across the M baseline + 12 markets; remediation history captured.</done>
</task>

</tasks>

<verification>
- `pnpm check:contrast` exits 0 with all 13 themes passing
- `pnpm type-check` exits 0
- `pnpm build` exits 0
- `03-CONTRAST-RESULTS.md` documents every market's pair ratios + any tweak applied
</verification>

<success_criteria>
ROADMAP §Phase 3 success criterion #4 (WCAG AA in every one of the 12 markets) auto-verified and re-runnable. Tweak history captured for the brand team.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mercados-sobre-m/03-01-SUMMARY.md`.
</output>
