---
phase: 03-mercados-sobre-m
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - scripts/check-markets.cjs
  - package.json
  - .planning/phases/03-mercados-sobre-m/03-UAT.md
autonomous: true
requirements: [REQ-markets-twelve]
must_haves:
  truths:
    - "scripts/check-markets.cjs validates each of the 12 markets has services array of valid ServiceIds, in canonical order, count matches the brief"
    - "Per-market service copy coverage is reported (≥80% expected; below that flagged but not failing)"
    - "package.json exposes 'check:markets'"
    - "03-UAT.md captures the 12-market recognisability sign-off + 48-state QA matrix as a one-page checklist"
  artifacts:
    - path: "scripts/check-markets.cjs"
      provides: "Service-list integrity check across all 12 markets"
      contains: "SIZE_MARKETS"
    - path: ".planning/phases/03-mercados-sobre-m/03-UAT.md"
      provides: "12-market recognisability + 48-state QA matrix sign-off"
      contains: "recognisable"
  key_links:
    - from: "pnpm check:markets"
      to: "12 SIZE_MARKETS service arrays"
      via: "parses src/data/size-data.ts as text + cross-references SIZE_SERVICES + SIZE_SERVICE_COPY"
      pattern: "SIZE_MARKETS"
---

<objective>
Phase 3 success criteria 1, 2, 3, and 5 — token integrity + service-list filtering + recognisability + transition smoke — need verification artifacts. SC #4 (WCAG AA) is closed by Plan 03-01. This plan adds the data-integrity script and the manual sign-off matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-mercados-sobre-m/03-CONTEXT.md
@.planning/phases/03-mercados-sobre-m/03-01-SUMMARY.md
@src/data/size-data.ts
@scripts/check-contrast.cjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/check-markets.cjs (service-list integrity)</name>
  <files>scripts/check-markets.cjs, package.json</files>
  <read_first>
    - src/data/size-data.ts (SIZE_MARKETS, SIZE_SERVICES, SIZE_SERVICE_COPY)
    - .planning/phases/03-mercados-sobre-m/03-CONTEXT.md (D-02: 4-step verification)
    - ExistingData/SIZE-design-brief-v4.md (§5 — services per market, used to validate counts)
  </read_first>
  <action>
    Create `scripts/check-markets.cjs` (pure Node, no third-party deps). Same parsing approach as check-contrast.cjs — read `src/data/size-data.ts` as text, extract:
    - Service ids from `SIZE_SERVICES` keys
    - Each market's `id` and `services` array
    - Each market's per-service copy keys from `SIZE_SERVICE_COPY[market.id]`

    For each of the 12 markets, run these checks:

    1. **Service id validity**: every entry in `services` is a key in `SIZE_SERVICES`. Fail loud on any orphan id.
    2. **No duplicates**: `services` has no repeated id.
    3. **Length sanity**: `services.length` is between 5 and 11 (sub-set of the 11 services). Out-of-band lengths fail.
    4. **Copy coverage**: count how many of the market's services have a copy entry in `SIZE_SERVICE_COPY[market.id]`. Coverage = covered / total. PASS ≥ 80%, WARN 60–80%, FAIL < 60%.

    Print a per-market block:
    ```
    cpg            services 6/11   copy 6/6 (100%)   PASS
    banca          services 8/11   copy 8/8 (100%)   PASS
    …
    ```

    OVERALL line:
    ```
    OVERALL  N/12 markets pass · X warn · Y fail
    ```

    Exit non-zero if any market is FAIL.

    Add `"check:markets": "node scripts/check-markets.cjs"` to package.json scripts (next to `check:contrast`).
  </action>
  <verify>
    <automated>pnpm check:markets 2>&1 | tee /tmp/markets.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/markets.txt','utf8'); const lines=t.split('\\n').filter(l=>/^[a-z]+\\s+services\\s+\\d+/i.test(l)); if(lines.length<12){console.error('expected 12 market lines, got',lines.length);process.exit(1)} if(!/OVERALL/.test(t)){console.error('missing OVERALL');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-markets.cjs` exists, pure Node
    - `pnpm check:markets` prints 12 market lines + OVERALL
    - Exits 0 when all markets PASS or WARN; non-zero on FAIL
    - No third-party deps
    - package.json has `check:markets` script
  </acceptance_criteria>
  <done>Service-list integrity is auto-verified and re-runnable on every size-data.ts edit.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke market token application + transitions</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - src/App.vue (token setProperty block, has-market class, reconfigure trigger)
    - src/styles/main.css (body transition rule + has-market muted/line derivation)
    - src/components/ReconfigureOverlay.vue (overlay z-index, animations)
  </read_first>
  <action>
    Confirm in the codebase (read-only):

    1. `src/App.vue` watches `[() => style.market, () => style.code, () => style.level]` and calls `setProperty` for `--mkt-primary, --mkt-secondary, --mkt-bg, --mkt-ink, --mkt-display, --mkt-body, --bg, --ink, --accent, --accent-2, --font-display, --font-body` when a market is active.
    2. `src/App.vue` adds `html.has-market` and removes `level-{x}` classes when a market is active.
    3. `src/App.vue` calls `pulseReconfigure()` on every market change (skipping the initial paint).
    4. `src/styles/main.css` has the `html.has-market { --muted: color-mix(...) }` rule that derives muted/line from `--ink` so the same body/muted contrast contract holds in market mode.
    5. `body { transition: background 600ms ease, color 600ms ease }` — same as Phase 2 confirmation.

    Print one OK / FAIL line per check. Exit non-zero on any FAIL. No code changes; if a regression is found, abort and surface.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const app=fs.readFileSync('src/App.vue','utf8'); const css=fs.readFileSync('src/styles/main.css','utf8'); const rc=fs.readFileSync('src/components/ReconfigureOverlay.vue','utf8'); const checks=[[/setProperty\\(['\"]--mkt-primary/, 'app sets --mkt-primary'],[/setProperty\\(['\"]--mkt-bg/, 'app sets --mkt-bg'],[/classList\\.add\\(['\"]has-market/, 'app adds has-market'],[/pulseReconfigure\\(/, 'app pulses Reconfigure on change'],[/html\\.has-market\\s*{[^}]*--muted/m, 'main.css derives muted under has-market'],[/transition:\\s*background\\s+600ms\\s+ease,\\s*color\\s+600ms\\s+ease/, 'main.css 600ms body transition']]; let ok=true; for(const [r,n] of checks){const pass=r.test(app)||r.test(css); console.log((pass?'OK':'FAIL')+' '+n); if(!pass)ok=false;} if(!ok)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - 6 OK lines printed; no FAIL
    - Exit 0
  </acceptance_criteria>
  <done>Market application + transition contract evidence-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Author 03-UAT.md (recognisability + 48-state matrix)</name>
  <files>.planning/phases/03-mercados-sobre-m/03-UAT.md</files>
  <read_first>
    - .planning/phases/03-mercados-sobre-m/03-CONTEXT.md (D-03: 12 rows × 5 columns matrix)
    - .planning/phases/03-mercados-sobre-m/03-CONTRAST-RESULTS.md (Plan 01 output — referenced from UAT)
    - src/data/size-data.ts (SIZE_MARKETS for the 12 ids and sub names)
  </read_first>
  <action>
    Create `03-UAT.md` with three sections:

    **A. Recognisability sign-off** — table with one row per market:

    | id | sub | recognisable as industry? | screenshot | reviewer | date |
    |----|-----|---------------------------|------------|----------|------|
    | cpg | CPG | [ ] yes / [ ] no — note | path | name | date |
    | banca | Finance | [ ] yes / [ ] no — note | path | name | date |
    | … (10 more) |

    Operator opens `pnpm dev`, picks each market from Home, snapshots `/servicios` and `/quienes-somos`, asks a non-technical reader "what kind of business is this?". Mark yes/no + note if no.

    **B. 48-state QA matrix** — 12 markets × 4 view-states (Home, Servicios, Quiénes somos, Contacto). Render as a checkbox grid:

    ```
                Home  Servicios  Quienes  Contacto
    cpg         [ ]   [ ]        [ ]      [ ]
    banca       [ ]   [ ]        [ ]      [ ]
    …
    ```

    Each cell ticks when the operator visually confirms the view renders without console errors, with WCAG AA holding (already auto-verified by check:contrast), and with the market personality coming through (palette + typography + Reconfigurando overlay on entry).

    **C. Transition smoke** — single line:

    > [ ] Switch CPG → Banca → Tech → Moda → exit market. Each transition feels instant-but-not-jarring; Reconfigurando overlay carries the gap (~900ms); no full reload, no flash beyond the overlay.

    **D. Sign-off** — table footer:

    | Reviewer | Date | All cells PASS? | Notes |
    |----------|------|-----------------|-------|
    | | | [ ] yes [ ] no | |

    Reference `03-CONTRAST-RESULTS.md` at the top so a reviewer can confirm WCAG AA before walking the matrix.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/03-mercados-sobre-m/03-UAT.md'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); const required=['cpg','banca','retail','automotriz','salud','bebidas','inmobiliario','educacion','turismo','tech','moda','startups','recognisable','Reconfigurando','03-CONTRAST-RESULTS']; for(const k of required){if(!s.toLowerCase().includes(k.toLowerCase())){console.error('missing',k);process.exit(1)}} const boxes=(s.match(/\\[ \\]/g)||[]).length; if(boxes<48){console.error('expected ≥48 checkboxes for the matrix, got',boxes);process.exit(1)} console.log('ok',boxes,'boxes');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - All 12 market ids appear
    - References `03-CONTRAST-RESULTS.md`
    - At least 48 checkboxes (the QA matrix grid)
    - Recognisability table + 48-state matrix + transition smoke + sign-off section all present
  </acceptance_criteria>
  <done>Phase 3 has a single document the operator signs off before Phase 4 planning.</done>
</task>

</tasks>

<verification>
- `pnpm check:contrast` and `pnpm check:markets` both exit 0
- `pnpm type-check` exits 0
- `pnpm build` exits 0
- `03-UAT.md` exists with the recognisability table, 48-state matrix, transition smoke, sign-off
</verification>

<success_criteria>
ROADMAP §Phase 3 success criteria 1 (12 token sets), 2 (filtered services), 3 (recognisability), 5 (~600ms transitions) are verified or staged for operator sign-off. Combined with Plan 03-01 (SC #4 WCAG AA), Phase 3 is ready to be marked done after the operator walks the UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mercados-sobre-m/03-02-SUMMARY.md`.
</output>
