---
phase: 06-tamano-xl-unleashed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-contrast.cjs
  - .planning/phases/06-tamano-xl-unleashed/06-CONTRAST-RESULTS.md
autonomous: true
requirements: [REQ-sizes-five]
must_haves:
  truths:
    - "scripts/check-contrast.cjs parses html.level-xl token block from main.css and runs the same 5-pair contract"
    - "All 17 themes (M + 12 markets + S + L + XS + XL) pass WCAG AA — full catalog complete"
    - "If XL fails on any pair, smallest-tweak remediation or structural override (DEC-041/052 pattern) is applied; >1 token change surfaces (DEC-018)"
    - "Final pnpm check:contrast exits 0 with OVERALL 17/17"
  artifacts:
    - path: "scripts/check-contrast.cjs"
      provides: "Extended to XL alongside M + 12 markets + S + L + XS"
      contains: "level-xl|'xl'"
    - path: ".planning/phases/06-tamano-xl-unleashed/06-CONTRAST-RESULTS.md"
      provides: "XL audit; documents that --accent-2 is decorative-only (gradient-masked)"
      contains: "WCAG AA"
  key_links:
    - from: "pnpm check:contrast"
      to: "html.level-xl token block in main.css"
      via: "parseLevelTokens('xl') reusing the existing parser"
      pattern: "html\\.level-xl"
---

<objective>
Extend the contrast checker to cover XL tokens. After this plan the SIZE catalog reaches its end state: 17/17 themes pass WCAG AA. Document the `--accent-2` decision (decorative-only inside gradient text — not a body-pair).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-tamano-xl-unleashed/06-CONTEXT.md
@scripts/check-contrast.cjs
@src/styles/main.css
@.planning/phases/05-tamano-xs-plain/05-CONTRAST-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check-contrast.cjs to XL</name>
  <files>scripts/check-contrast.cjs</files>
  <read_first>
    - scripts/check-contrast.cjs (current — 16 themes; uses parseLevelTokens / buildLevelPairs / runLevel for S, L, XS)
    - src/styles/main.css (html.level-xl block lines 146-153 — read property values verbatim)
    - .planning/phases/06-tamano-xl-unleashed/06-CONTEXT.md (D-06: --accent-2 decorative-only)
  </read_first>
  <action>
    Add `'xl'` to the level array. The script's existing parser handles --accent; --accent-2 is NOT a body-pair surface (it's only used inside the `xl-grad-text` linear-gradient where the gradient interpolates between accent and accent-2 and is then `background-clip: text` — the rendered color is not a single hex value, so a body-pair check makes no sense). Skip --accent-2 in the XL contract.

    Header / usage updates:
    - Header (lines ~6-12): "Total: 17 themes (M + 12 markets + S + L + XS + XL)".
    - `--levels-only` flag prints 5 themes (M + S + L + XS + XL).
    - OVERALL line reads `N/17`.

    Sanity-check expected XL pairs (compute mentally):
    - body (INK on BG) = #fff on #050505 → ~20:1 PASS (>= 4.5)
    - muted (MUTED on BG) = #aaa on #050505 → ~9:1 PASS (>= 4.5)
    - large heading (INK on BG) = ~20:1 PASS (>= 3.0)
    - accent CTA (BG on ACCENT) = #050505 on #00ffaa → ~17:1 PASS (>= 4.5)
    - accent inline (ACCENT on BG) = #00ffaa on #050505 → ~17:1 PASS (>= 3.0)

    XL is the easiest level — all 5 pairs should pass cleanly with no remediation.
  </action>
  <verify>
    <automated>node scripts/check-contrast.cjs 2>&1 | tee /tmp/contrast.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/contrast.txt','utf8'); if(!/SIZE — XL\b/.test(t)){console.error('missing XL block');process.exit(1)} if(!/OVERALL\s+\d+\/17/.test(t)){console.error('OVERALL not /17');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Script prints an XL block alongside M, 12 markets, S, L, XS
    - OVERALL line reads `N/17`
    - `--levels-only` flag prints 5 blocks (M + S + L + XS + XL)
    - Header / usage comments updated to "17 themes"
    - Token values are READ from main.css, not duplicated
  </acceptance_criteria>
  <done>The contrast contract covers the entire SIZE catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Document audit + --accent-2 rationale</name>
  <files>.planning/phases/06-tamano-xl-unleashed/06-CONTRAST-RESULTS.md</files>
  <read_first>
    - Output of pnpm check:contrast from Task 1
    - src/styles/main.css (html.level-xl block + xl-grad-text rule lines 161-168)
    - .planning/phases/06-tamano-xl-unleashed/06-CONTEXT.md (D-06)
    - .planning/phases/05-tamano-xs-plain/05-CONTRAST-RESULTS.md (mirror format)
  </read_first>
  <action>
    Author `06-CONTRAST-RESULTS.md` mirroring `05-CONTRAST-RESULTS.md`:

    ```markdown
    # Phase 6 — XL WCAG AA contrast audit

    Generated: <date>
    Source: pnpm check:contrast against src/styles/main.css level-xl block

    ## XL (Unleashed) baseline
    | pair | ratio | result |
    | body | ~20:1 | PASS |
    | muted | ~9:1 | PASS |
    | large heading | ~20:1 | PASS |
    | accent CTA | ~17:1 | PASS |
    | accent inline | ~17:1 | PASS |

    ## --accent-2 (#FF00FF) note
    --accent-2 is decorative-only: it is referenced ONLY inside the
    `html.level-xl .xl-grad-text` rule where the value is one stop
    of a linear-gradient that is then `-webkit-background-clip: text`
    — the rendered text color is not a single hex value but a moving
    gradient. A body-pair contrast check on --accent-2 would be
    meaningless. Captured here for auditability.

    ## Tweaks applied
    None. XL passes 5/5 on the first run with zero token changes.
    ```
  </action>
  <verify>
    <automated>pnpm check:contrast 2>&1 | tee /tmp/contrast.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/contrast.txt','utf8'); if(!/OVERALL\s+17\/17 themes pass/.test(t)){console.error('not all 17 pass:\n',t);process.exit(1)} console.log('all 17 pass');"; node -e "const fs=require('fs'); const p='.planning/phases/06-tamano-xl-unleashed/06-CONTRAST-RESULTS.md'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); if(!/XL \(Unleashed\)/.test(s)){console.error('missing XL section');process.exit(1)} if(!/accent-2/.test(s)){console.error('missing --accent-2 rationale');process.exit(1)} console.log('audit covers XL');"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm check:contrast exits 0 with OVERALL 17/17
    - 06-CONTRAST-RESULTS.md exists with XL section + --accent-2 rationale
    - DEC-006 / DECISION-XL-PHASER tokens (#050505, #fff, #00ffaa, #ff00ff) untouched
    - pnpm type-check + pnpm build pass
  </acceptance_criteria>
  <done>XL passes; the 17-style contrast contract is closed.</done>
</task>

</tasks>

<verification>
- pnpm check:contrast exits 0 with OVERALL 17/17
- pnpm type-check + pnpm build pass
- 06-CONTRAST-RESULTS.md captures the audit
</verification>

<success_criteria>
ROADMAP §Phase 6 success criterion #3 (WCAG AA in XL) auto-verified at the token level. WebGL2 gate + L fallback in 06-02; HomeView XL branch in 06-03; UAT in 06-04.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tamano-xl-unleashed/06-01-SUMMARY.md`.
</output>
