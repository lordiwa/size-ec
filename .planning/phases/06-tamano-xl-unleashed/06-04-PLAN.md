---
phase: 06-tamano-xl-unleashed
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - .planning/phases/06-tamano-xl-unleashed/06-UAT.md
autonomous: true
requirements: [REQ-sizes-five, REQ-xl-capability-detection]
must_haves:
  truths:
    - "06-UAT.md captures Home-XL visual check + WebGL2 fallback test + bundle audit + reduced-motion + transition smoke + sign-off"
    - "References 06-CONTRAST-RESULTS.md so reviewer confirms 17/17 WCAG AA before walking the matrix"
    - "Documents DEC-060 (Three.js scene + Phaser deferred to Phase 7) and DEC-061 (no XL branch in 4 protected views) as expected-not-bug"
  artifacts:
    - path: ".planning/phases/06-tamano-xl-unleashed/06-UAT.md"
      provides: "Operator sign-off doc for Phase 6"
      contains: "XL (Unleashed)"
  key_links:
    - from: "06-UAT.md"
      to: "06-CONTRAST-RESULTS.md + 06-02-SUMMARY.md"
      via: "explicit references at top"
      pattern: "06-CONTRAST-RESULTS"
---

<objective>
Phase 6 ships infrastructure (capability detection, fallback, gradient identity). The operator confirms it works end-to-end, including the negative path (WebGL2 disabled).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-tamano-xl-unleashed/06-CONTEXT.md
@.planning/phases/06-tamano-xl-unleashed/06-01-SUMMARY.md
@.planning/phases/06-tamano-xl-unleashed/06-02-SUMMARY.md
@.planning/phases/06-tamano-xl-unleashed/06-03-SUMMARY.md
@.planning/phases/05-tamano-xs-plain/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06-UAT.md sign-off matrix</name>
  <files>.planning/phases/06-tamano-xl-unleashed/06-UAT.md</files>
  <read_first>
    - .planning/phases/05-tamano-xs-plain/05-UAT.md (mirror format)
    - .planning/phases/06-tamano-xl-unleashed/06-CONTEXT.md (D-08 — UAT structure)
    - .planning/phases/06-tamano-xl-unleashed/06-CONTRAST-RESULTS.md (referenced)
  </read_first>
  <action>
    Create `06-UAT.md` with sections A–E, ≥10 checkboxes:

    **Header** — references `06-CONTRAST-RESULTS.md` (17/17) and `06-02-SUMMARY.md` (capability contract).

    **A. Home-XL visual check** — operator opens `pnpm dev` on a WebGL2-capable browser, picks XL from the footer mini-slider:
    - [ ] Reconfigurando overlay played on entry
    - [ ] Runtime label `[ home.scene · runtime ]` visible top-left in mono uppercase, neon green
    - [ ] SIZE wordmark renders with the animated gradient (cycles slowly through green/magenta/green)
    - [ ] Tagline "Publicidad a tu medida." readable in the display font
    - [ ] Rotating word "Somos tu [palabra]." — the word picks up the gradient effect
    - [ ] MarketSelect dropdown renders below, accessible

    **B. WebGL2 fallback test** — operator opens DevTools, disables WebGL2 (Chrome: chrome://flags or DevTools "Rendering" panel "Emulate" → "no WebGL2"; Firefox: about:config webgl.disabled = true). Hard-reload. Pick XL:
    - [ ] Style flips to L automatically (no XL render)
    - [ ] Toast appears bottom-right: "XL no disponible. Tu navegador no soporta WebGL2 — caímos a L (Bold) automáticamente."
    - [ ] Toast has a `×` close button that dismisses immediately
    - [ ] No console errors / no black screen
    - [ ] After ~3.5s without manual dismiss, toast auto-fades

    **C. Bundle audit (REQ-progressive-loading scaffold)** — operator runs `pnpm build` and inspects `dist/assets/`:
    - [ ] No chunk filename contains `three`, `phaser`, `tone`, or `postprocessing` (Phase 6 ships the contract; Phase 7 will install these and re-run this check with bundle-splitting verification)

    **D. Reduced-motion sweep** — single line:
    > [ ] Toggle OS-level reduced motion. The XL gradient animation freezes (the `xl-grad` keyframes pause via the global `animation-duration: 0.01ms` rule). The rotating word still swaps (text mutation, not motion). Reconfigurando overlay still flashes.

    **E. Transition smoke** — single line:
    > [ ] Pick M → XL → M from the footer mini-slider. Each transition: ~600ms body bg/ink fade, Reconfigurando overlay covers ~900ms, then the new layout commits. No full reload.

    **F. Sign-off table** — Reviewer / Date / All cells PASS? / Notes.

    Add a footnote calling out **DEC-060** (Three.js scene + Phaser mini-game intentionally deferred to Phase 7) and **DEC-061** (Servicios / Quiénes / Cliente / Contacto inherit M-default in XL — not a missing piece, an explicit decision).

    Total ≥10 checkboxes (5-6 visual + 5 fallback + 1 bundle + 1 reduced-motion + 1 transition).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/06-tamano-xl-unleashed/06-UAT.md'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); const required=['06-CONTRAST-RESULTS','XL','Home','WebGL2','Reconfigurando','prefers-reduced-motion','xl-grad','DEC-060','DEC-061']; for(const k of required){if(!s.toLowerCase().includes(k.toLowerCase())){console.error('missing',k);process.exit(1)}} const boxes=(s.match(/\[ \]/g)||[]).length; if(boxes<10){console.error('expected ≥10 checkboxes, got',boxes);process.exit(1)} console.log('ok',boxes,'boxes');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all required sections
    - Mentions XL, Home, WebGL2, Reconfigurando, prefers-reduced-motion, xl-grad
    - References 06-CONTRAST-RESULTS.md
    - Mentions DEC-060 + DEC-061 footnotes
    - At least 10 checkboxes
    - Sign-off table at the bottom
  </acceptance_criteria>
  <done>Phase 6 ready for operator sign-off; Phase 7 (Contenido + Three.js / Phaser stack) unblocked.</done>
</task>

</tasks>

<verification>
- 06-UAT.md exists with ≥10 checkboxes
- Mentions XL, WebGL2, Reconfigurando, DEC-060, DEC-061
- References 06-CONTRAST-RESULTS.md
</verification>

<success_criteria>
Operator can sign off Phase 6 in one walk + one DevTools session: WCAG AA already auto-verified, XL identity visually confirmed, fallback path verified end-to-end, bundle contract documented.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tamano-xl-unleashed/06-04-SUMMARY.md`.
</output>
