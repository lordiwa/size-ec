---
phase: 05-tamano-xs-plain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-contrast.cjs
  - .planning/phases/05-tamano-xs-plain/05-CONTRAST-RESULTS.md
autonomous: true
requirements: [REQ-sizes-five]
must_haves:
  truths:
    - "scripts/check-contrast.cjs parses html.level-xs token block from main.css and runs the same 5-pair contract per level"
    - "All 16 themes (M + 12 markets + S + L + XS) pass WCAG AA"
    - "If XS fails on any pair, smallest-tweak remediation is applied to main.css; >1 token change surfaces to the user before commit (DEC-018 pattern)"
    - "Final pnpm check:contrast exits 0 with OVERALL 16/16"
  artifacts:
    - path: "scripts/check-contrast.cjs"
      provides: "Extended to XS level alongside M baseline + 12 markets + S + L"
      contains: "level-xs|'xs'"
    - path: ".planning/phases/05-tamano-xs-plain/05-CONTRAST-RESULTS.md"
      provides: "XS audit with before/after if any token tuned"
      contains: "WCAG AA"
  key_links:
    - from: "pnpm check:contrast"
      to: "html.level-xs token block in main.css"
      via: "parseLevelTokens('xs') reusing the existing parser"
      pattern: "html\\.level-xs"
---

<objective>
Extend the contrast checker to cover XS tokens. Surface and remediate any sub-AA pair. Phase 5 cannot ship XS UI without WCAG AA proven on the Web 1999 palette (gray bg + black ink + red accent + blue links + purple visited).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tamano-xs-plain/05-CONTEXT.md
@scripts/check-contrast.cjs
@src/styles/main.css
@.planning/phases/04-tamanos-s-y-l/04-CONTRAST-RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check-contrast.cjs to XS</name>
  <files>scripts/check-contrast.cjs</files>
  <read_first>
    - scripts/check-contrast.cjs (current — M + 12 markets + S + L; uses parseLevelTokens / buildLevelPairs / runLevel for S and L)
    - src/styles/main.css (html.level-xs block lines 121-144 — read property values verbatim)
    - .planning/phases/05-tamano-xs-plain/05-CONTEXT.md (D-04: same parser, mechanical addition)
    - .planning/phases/04-tamanos-s-y-l/04-CONTRAST-RESULTS.md (DEC-041 override pattern — XS likely won't need it; documented for reference)
  </read_first>
  <action>
    Add `'xs'` to the level array the script iterates over. The existing `parseLevelTokens(cssSrc, 'xs')` and `runLevel(...)` functions handle the rest mechanically.

    Key acceptance rules:
    - Header comment (lines ~6-12) updates from "Total: 15 themes" to "Total: 16 themes (M + 12 markets + S + L + XS)".
    - Usage comment (lines ~17-19) is updated for the new theme count.
    - `--levels-only` flag now prints M + S + L + XS (4 themes).
    - The OVERALL line reads `N/16`. Exit non-zero if any fail.
    - No new dependencies. Pure Node.

    Sanity-check the expected XS pairs at WCAG AA (compute mentally):
    - body (INK on BG) = #000 on #c0c0c0 → ~12.6:1 PASS (>= 4.5)
    - muted (MUTED on BG) = #444 on #c0c0c0 → ~5.7:1 PASS (>= 4.5)
    - large heading (INK on BG) = ~12.6:1 PASS (>= 3.0)
    - accent CTA (BG on ACCENT) = #c0c0c0 on #ff0000 → ~3.0:1 borderline; may PASS or FAIL the 4.5 body threshold
    - accent inline (ACCENT on BG) = #ff0000 on #c0c0c0 → same ~3.0:1; PASSES the 3.0 large-text threshold cleanly

    The accent CTA pair is the risk. If it fails, this is also closeable structurally (DEC-041 pattern from L): in XS the accent is decorative-only — links use `#0000ee` (blue) which has its own pair that easily clears AA. The "CTA" in XS UI is a plain `<a>` link, not a colored button. Surface to Task 2 if the script reports any XS fail.
  </action>
  <verify>
    <automated>node scripts/check-contrast.cjs 2>&1 | tee /tmp/contrast.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/contrast.txt','utf8'); if(!/SIZE — XS\b/.test(t)){console.error('missing XS block');process.exit(1)} if(!/OVERALL\s+\d+\/16/.test(t)){console.error('OVERALL not /16');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Script prints an XS block alongside M, 12 markets, S, L
    - OVERALL line reads `N/16`
    - `--levels-only` flag prints M + S + L + XS (4 blocks)
    - Header + usage comments updated to "16 themes"
    - Token values are READ from main.css, not duplicated in the script
  </acceptance_criteria>
  <done>The contrast contract covers all the styles Phase 5 ships.</done>
</task>

<task type="auto">
  <name>Task 2: Triage XS failures + remediate (or document zero-tweak pass)</name>
  <files>src/styles/main.css, .planning/phases/05-tamano-xs-plain/05-CONTRAST-RESULTS.md</files>
  <read_first>
    - Output of pnpm check:contrast from Task 1
    - src/styles/main.css (html.level-xs block lines 121-144)
    - .planning/phases/05-tamano-xs-plain/05-CONTEXT.md (DECISION-XS-RETRO — primary XS palette is locked to 1999 web defaults)
    - .planning/phases/04-tamanos-s-y-l/04-CONTRAST-RESULTS.md (remediation pattern + DEC-041 structural override pattern)
  </read_first>
  <action>
    Two paths:

    **Path A — XS passes 5/5 on first run.** Document baseline in `05-CONTRAST-RESULTS.md` (no tweaks); report "0 token changes". Most likely path given the math.

    **Path B — One pair fails (likely accent CTA #c0c0c0 on #ff0000 ≈ 3.0:1 vs 4.5 threshold).** Two sub-options:
    1. **Structural override (DEC-041 pattern).** Keep accent palette locked, change the contrast contract to reflect that XS has no colored CTA — links (the only interactive accent) use `#0000ee` on `#c0c0c0` which is 8.6:1 PASS. Override `accent CTA` for XS to "LINK on BG" = #0000ee on #c0c0c0. Override `accent inline` similarly if the script's default contract doesn't fit. Zero token changes; locked palette preserved.
    2. **Token tweak (DEC-018 pattern).** Darken `--accent` from `#ff0000` to `#cc0000` (~5.5:1 on gray). Single token change. DECISION-XS-RETRO says "red accent" without locking the exact hex — `#cc0000` is still a 1999-standard red.

    Prefer 1 (structural override) — keeps `#ff0000` exact for visual identity and uses links as the actual CTA, which is correct semantically for the 1999 vocabulary anyway.

    If a level needs >1 token change, STOP and surface — DEC-018.

    Document the audit in `05-CONTRAST-RESULTS.md` mirroring `04-CONTRAST-RESULTS.md`:

    ```markdown
    # Phase 5 — XS WCAG AA contrast audit

    Generated: <date>
    Source: pnpm check:contrast against src/styles/main.css level-xs block + link/visited overrides

    ## XS (Plain) baseline
    | pair | before | after | result |
    | body | 12.6:1 | -- | PASS |
    ...

    ## Remediation (if any)
    | token | before | after | reason | DEC ref |
    ```

    Final state: pnpm check:contrast exits 0 with `OVERALL 16/16`.
  </action>
  <verify>
    <automated>pnpm check:contrast 2>&1 | tee /tmp/contrast.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/contrast.txt','utf8'); if(!/OVERALL\s+16\/16 themes pass/.test(t)){console.error('not all 16 pass:\n',t);process.exit(1)} console.log('all 16 pass');"; node -e "const fs=require('fs'); const p='.planning/phases/05-tamano-xs-plain/05-CONTRAST-RESULTS.md'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); if(!/XS \(Plain\)/.test(s)){console.error('missing XS section');process.exit(1)} console.log('audit covers XS');"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm check:contrast exits 0 with OVERALL 16/16
    - 05-CONTRAST-RESULTS.md exists with an XS section
    - Any tweaks documented before/after
    - DECISION-XS-RETRO tokens (#c0c0c0, #000000, #0000ee) untouched (or surfaced if forced)
    - pnpm type-check + pnpm build pass
  </acceptance_criteria>
  <done>XS passes WCAG AA on the same 5-pair contract.</done>
</task>

</tasks>

<verification>
- pnpm check:contrast exits 0 with OVERALL 16/16
- pnpm type-check + pnpm build pass
- 05-CONTRAST-RESULTS.md captures the audit
</verification>

<success_criteria>
ROADMAP §Phase 5 success criterion #4 (WCAG AA legibility holds in XS) auto-verified at the token level. Per-view 1999 markup is wired in 05-02; 05-03 captures the manual UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tamano-xs-plain/05-01-SUMMARY.md`.
</output>
