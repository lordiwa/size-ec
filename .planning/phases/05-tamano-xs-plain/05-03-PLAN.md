---
phase: 05-tamano-xs-plain
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - .planning/phases/05-tamano-xs-plain/05-UAT.md
autonomous: true
requirements: [REQ-sizes-five]
must_haves:
  truths:
    - "05-UAT.md captures a 5×1 view-state matrix (5 views × XS) with PASS/FAIL per cell"
    - "1999-look highlights documented as separate checkboxes — Times New Roman renders, links are blue + underlined, marquee scrolls on Home, table borders are 1px solid, footer Win95 bevels respond to click"
    - "Reduced-motion sweep documented — verifies the <marquee> freezes under prefers-reduced-motion; rotator (text) keeps swapping"
    - "Transition smoke documented — switching M → XS → M each completes in ~600ms with the Reconfigurando overlay"
    - "References 05-CONTRAST-RESULTS.md so reviewer confirms WCAG AA before walking the matrix"
  artifacts:
    - path: ".planning/phases/05-tamano-xs-plain/05-UAT.md"
      provides: "Per-view sign-off matrix for XS on top of M baseline"
      contains: "XS (Plain)"
  key_links:
    - from: "05-UAT.md"
      to: "05-CONTRAST-RESULTS.md"
      via: "explicit reference at top"
      pattern: "05-CONTRAST-RESULTS"
---

<objective>
Phase 5 ships a treatment that demands a human eye on the 1999 look. This plan produces the operator's sign-off doc — Phase 5 isn't done until the matrix is ticked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-tamano-xs-plain/05-CONTEXT.md
@.planning/phases/05-tamano-xs-plain/05-01-SUMMARY.md
@.planning/phases/05-tamano-xs-plain/05-02-SUMMARY.md
@.planning/phases/04-tamanos-s-y-l/04-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 05-UAT.md sign-off matrix</name>
  <files>.planning/phases/05-tamano-xs-plain/05-UAT.md</files>
  <read_first>
    - .planning/phases/04-tamanos-s-y-l/04-UAT.md (mirror format)
    - .planning/phases/05-tamano-xs-plain/05-CONTEXT.md (D-07: 5×1 + highlights + reduced-motion + transition + sign-off)
    - .planning/phases/05-tamano-xs-plain/05-CONTRAST-RESULTS.md (referenced from this UAT)
  </read_first>
  <action>
    Create `05-UAT.md` with sections:

    **Header** — references `05-CONTRAST-RESULTS.md` for the auto-verified WCAG AA contract on the XS palette.

    **A. 5-view XS matrix** — single-column checkbox grid (operator opens `pnpm dev`, picks XS from the footer slider, walks each view):

    ```
                        XS
    Home                [ ]
    Servicios           [ ]
    Quiénes somos       [ ]
    Cliente             [ ]
    Contacto            [ ]
    ```

    Each cell: confirms the view renders without console errors, the 1999 personality reads (Times New Roman serif, gray bg, blue underlined links, table layout where applicable), the Reconfigurando overlay played on entry, and WCAG AA holds (already auto-confirmed for tokens; this is a visual sanity check).

    **B. 1999-look highlights** — 5-7 checkboxes for the marker behaviors that prove the look is literal, not a retro accent:
    - [ ] Home renders ★ SIZE ★ heading in Times New Roman, italic tagline, yellow `<marquee>` scrolling at the top
    - [ ] Servicios renders all 11 services in a single `<table>` with 1px solid black borders, no cards, no flex/grid
    - [ ] Quiénes somos renders the team grid as a `<table>` (DevTools → Elements confirms `<TABLE>` tag, not `<DIV>` with grid)
    - [ ] Cliente renders one `<table>` per cliente
    - [ ] Contacto renders channels + chatbot in a 2-col `<table>` and the form aligns labels/inputs via `<table>` rows
    - [ ] Internal links (RouterLink) render as plain blue underlined anchors; visited links go purple
    - [ ] Footer mini-slider in XS uses Win95 bevel (raised buttons, inset on click)

    **C. Reduced-motion sweep** — single line:
    > [ ] Toggle OS-level reduced motion. The Home `<marquee>` freezes mid-scroll. The rotating word on Home keeps swapping (it's text, not motion). Reconfigurando overlay still flashes. Footer bevel buttons still depress (1ms transition is below human perception).

    **D. Transition smoke** — single line:
    > [ ] Pick M → XS from the footer mini-slider, then XS → M. Each transition: ~600ms body bg/ink fade, Reconfigurando overlay covers ~900ms, then the new layout commits. No full reload, no flash beyond the overlay.

    **E. Sign-off table** — Reviewer / Date / All cells PASS? / Notes / DEC-### escalation flag (if 05-01 used a structural override or token tweak).

    Total ≥12 checkboxes (5 matrix + 5-7 highlights + 1 reduced-motion + 1 transition smoke).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/05-tamano-xs-plain/05-UAT.md'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); const required=['05-CONTRAST-RESULTS','XS','Home','Servicios','Quiénes','Cliente','Contacto','Reconfigurando','prefers-reduced-motion','marquee','Times New Roman']; for(const k of required){if(!s.toLowerCase().includes(k.toLowerCase())){console.error('missing',k);process.exit(1)}} const boxes=(s.match(/\[ \]/g)||[]).length; if(boxes<12){console.error('expected ≥12 checkboxes, got',boxes);process.exit(1)} console.log('ok',boxes,'boxes');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all required sections
    - Mentions all 5 view names + XS
    - References 05-CONTRAST-RESULTS.md
    - At least 12 checkboxes
    - Sign-off table at the bottom
  </acceptance_criteria>
  <done>Phase 5 has a single document the operator signs off before Phase 6 (XL) planning.</done>
</task>

</tasks>

<verification>
- 05-UAT.md exists with ≥12 checkboxes
- All 5 view names + XS appear
- References 05-CONTRAST-RESULTS.md
</verification>

<success_criteria>
Operator can sign off Phase 5 in one walk: WCAG AA already auto-verified, XS 1999 treatments visually confirmed across the 5 views, reduced-motion + transition behaviour confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tamano-xs-plain/05-03-SUMMARY.md`.
</output>
