---
phase: 05-tamano-xs-plain
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/views/HomeView.vue
  - src/views/ServiciosView.vue
  - src/views/QuienesSomosView.vue
  - src/views/ClienteView.vue
  - src/views/ContactoView.vue
autonomous: true
requirements: [REQ-sizes-five]
must_haves:
  truths:
    - "HomeView XS branch matches prototype 00021082_04 lines 23-37 verbatim: <center> + ★ SIZE ★ heading + italic tagline + <marquee> + <hr> + 'Somos tu [rotator]' + native MarketSelect"
    - "ServiciosView XS branch renders the 11 canonical services as a single <table border=\"1\"> — no cards, no grid"
    - "QuienesSomosView XS branch uses <table> for the 4×5 photo grid + a separate <table> for clients"
    - "ClienteView XS branch uses one <table> per cliente listing trabajos as rows"
    - "ContactoView XS branch uses a 2-column <table> for channels + chatbot placeholder, then a <form> with <table> for label/input alignment"
    - "XS branches use HTML 1999 vocabulary only: no Tailwind classes, no display:flex/grid, no border-radius, no box-shadow, no transform, no transition in scoped styles (DEC-050)"
    - "XS branches sit alongside the existing M/S/L markup — order in HomeView is L → S → XS → M-default"
    - "Reduced-motion global rule still freezes the <marquee> automatically (no per-component override added)"
  artifacts:
    - path: "src/views/HomeView.vue"
      provides: "Per-level home layouts (M / S / L / XS)"
      contains: "style.code === 'xs'"
    - path: "src/views/ServiciosView.vue"
      provides: "XS branch with table-of-services"
      contains: "<table"
  key_links:
    - from: "Each protected view"
      to: "v-if XS branch"
      via: "v-if='style.code === \\'xs\\''"
      pattern: "&lt;section v-if=\"style.code === 'xs'\"&gt;"
---

<objective>
Ship the prototype's XS treatment across the 5 views by wiring per-view template branches that use HTML 1999 vocabulary (`<table>`, `<center>`, `<hr>`, `<marquee>`, inline styles) — not Tailwind, not Grid, not modern CSS effects. Match `ExistingData/prototype-extracted/*.js` Home XS branch verbatim; design the other 4 views inside the same vocabulary because the prototype only specifies Home XS literally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-tamano-xs-plain/05-CONTEXT.md
@.planning/phases/05-tamano-xs-plain/05-01-SUMMARY.md
@ExistingData/prototype-extracted/00021082_04_a353c98e-7080-4c7c-859d-e8ebafc9597d.js
@ExistingData/prototype-extracted/00021163_02_c6135f0c-28a8-4eb5-9e32-e33aa46e7bc4.js
@ExistingData/prototype-extracted/00021244_01_86c85a12-94ee-473b-b21e-d8227f98510a.js
@ExistingData/prototype-extracted/00021325_07_6e363be8-180c-48b7-a8e2-f91c5b740b91.js
@ExistingData/prototype-extracted/00021406_03_7e82b5d6-c24b-4074-9bfa-0169e271158c.js
@src/views/HomeView.vue
@src/views/ServiciosView.vue
@src/views/QuienesSomosView.vue
@src/views/ClienteView.vue
@src/views/ContactoView.vue
@src/components/MarketSelect.vue
@src/components/RotatingWord.vue
@src/components/PortraitPlaceholder.vue
@src/data/size-data.ts
@src/styles/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeView XS branch (verbatim port from prototype)</name>
  <files>src/views/HomeView.vue</files>
  <read_first>
    - ExistingData/prototype-extracted/00021082_04_*.js lines 23-37 (Home XS branch verbatim)
    - src/views/HomeView.vue (current — L / S / M branches)
    - src/components/MarketSelect.vue
    - src/components/RotatingWord.vue
  </read_first>
  <action>
    Insert a new `v-else-if="style.code === 'xs'"` branch in HomeView.vue between the existing S branch and the final M `v-else`. New order: **L → S → XS → M-default**.

    Markup (Vue equivalent of the prototype JSX):

    ```vue
    <!-- XS (Plain) — Web 1999 literal: <center>, <marquee>, <hr>, Times New Roman.
         DO NOT add Tailwind classes, flex/grid, border-radius, or box-shadow here. -->
    <section v-else-if="style.code === 'xs'" class="home-xs" aria-live="polite">
      <center>
        <h1 class="home-xs-mark">★ SIZE ★</h1>
        <p><i>Publicidad a tu medida.</i></p>
        <marquee scrollamount="6" class="home-xs-marquee">
          ✦ Somos tu <RotatingWord :words="words" :idx="wIdx" /> ✦ Bienvenido a SIZE Agency Inc. ✦
        </marquee>
        <hr />
        <p class="home-xs-line">
          Somos tu <b><RotatingWord :words="words" :idx="wIdx" /></b>.
        </p>
        <div class="home-xs-cta">
          <MarketSelect :code="style.code" :value="style.marketId" @pick="pickMarket" />
        </div>
      </center>
    </section>
    ```

    Add scoped CSS for `.home-xs`, `.home-xs-mark`, `.home-xs-marquee`, `.home-xs-line`, `.home-xs-cta` using only basic CSS:
    - `.home-xs { padding: 12px; font-family: "Times New Roman", serif; }`
    - `.home-xs-mark { font-size: 48px; margin: 8px 0; }`
    - `.home-xs-marquee { background: #ffff00; border: 2px solid #000; padding: 4px; max-width: 760px; }`
    - `.home-xs-line { white-space: nowrap; }`
    - `.home-xs-cta { margin-top: 16px; }`

    Do NOT delete L, S, or M branches. M `v-else` remains the catch-all (XL still falls through to M; market mode also lands here).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/views/HomeView.vue','utf8'); const checks=[/style\.code === ['\"]xs['\"]/, /class=\"home-xs\"/, /&lt;marquee/i, /★ SIZE ★/, /home-xs-marquee/, /font-family:.*Times New Roman/i]; for(const c of checks){if(!c.test(s)){console.error('missing',c);process.exit(1)}} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - HomeView has a `v-else-if="style.code === 'xs'"` branch
    - Renders `<center>`, `<marquee>`, `<hr>`, ★ SIZE ★ heading, italic tagline
    - Reuses `<RotatingWord>` and `<MarketSelect>` (no new component)
    - Scoped styles use only basic CSS (no flex/grid/transform/transition/border-radius/box-shadow)
    - L / S / M branches unchanged
    - pnpm type-check passes
    - pnpm build passes
  </acceptance_criteria>
  <done>Home renders the prototype's 1999 XS branch.</done>
</task>

<task type="auto">
  <name>Task 2: ServiciosView XS branch (table of services)</name>
  <files>src/views/ServiciosView.vue</files>
  <read_first>
    - ExistingData/prototype-extracted/00021163_02_*.js (servicios — no explicit XS branch in the prototype; use 1999 vocabulary)
    - src/views/ServiciosView.vue (current — has CASE A / CASE B for market vs no-market)
    - src/data/size-data.ts (SIZE_SERVICES: 11 services with id, n, name, short)
  </read_first>
  <action>
    Wrap the existing top-level `<template v-if="!style.market">` and `<section v-else>` blocks inside a new outer condition. Add a sibling `v-if="style.code === 'xs'"` block above them.

    Pattern:
    ```vue
    <template>
      <!-- XS (Plain) — Web 1999 literal table-of-services -->
      <section v-if="style.code === 'xs'" class="srv-xs">
        <center>
          <h1 class="srv-xs-mark">SIZE</h1>
          <h2 class="srv-xs-h2">— SERVICIOS —</h2>
        </center>
        <table border="1" cellpadding="6" cellspacing="0" class="srv-xs-table">
          <thead>
            <tr><th>#</th><th>Servicio</th><th>Descripción</th></tr>
          </thead>
          <tbody>
            <tr v-for="s in allServices" :key="s.id">
              <td>{{ s.n }}</td>
              <td><b>{{ s.name }}</b></td>
              <td><i>{{ s.short }}</i></td>
            </tr>
          </tbody>
        </table>
        <p class="srv-xs-foot">
          ¿Quieres ver solo los servicios que aplican a tu industria?<br/>
          <RouterLink :to="{ name: 'home' }">Vuelve al inicio</RouterLink> y elige una categoría.<br/>
          <hr/>
          <RouterLink :to="{ name: 'contacto' }">Hablemos →</RouterLink>
        </p>
      </section>

      <!-- M / S / L (Phase 4) — existing markup, untouched -->
      <template v-else>
        <!-- … existing CASE A + CASE B blocks … -->
      </template>
    </template>
    ```

    Scoped CSS — basic only:
    - `.srv-xs { padding: 16px; font-family: "Times New Roman", serif; }`
    - `.srv-xs-mark { font-size: 56px; margin: 8px 0; letter-spacing: 4px; }`
    - `.srv-xs-h2 { font-size: 20px; font-weight: normal; margin: 0 0 16px; }`
    - `.srv-xs-table { margin: 0 auto; max-width: 720px; width: 100%; border-collapse: collapse; }`
    - `.srv-xs-table th { background: #ffffff; font-weight: bold; padding: 8px; text-align: left; border: 1px solid #000; }`
    - `.srv-xs-table td { padding: 8px; vertical-align: top; border: 1px solid #000; }`
    - `.srv-xs-foot { text-align: center; padding: 16px 0; }`

    Do NOT touch the existing CASE A (no-market) or CASE B (market chosen) blocks — they retain Phase 4's `.l-bold` treatments.

    Note: `RouterLink` in XS renders as a plain anchor. The global `html.level-xs a { color: #0000ee; text-decoration: underline }` rule applies, so it auto-styles as a 1999 link.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/views/ServiciosView.vue','utf8'); const checks=[/v-if=\"style\.code === 'xs'\"/, /class=\"srv-xs\"/, /&lt;table/i, /border=\"1\"/, /allServices/, /Times New Roman/]; for(const c of checks){if(!c.test(s)){console.error('missing',c);process.exit(1)}} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - ServiciosView has a `v-if="style.code === 'xs'"` branch
    - Renders `<table border="1">` listing all 11 services
    - No `display: flex` / `display: grid` / `border-radius` / `box-shadow` in the .srv-xs scoped CSS
    - Existing M / S / L markup unchanged (now inside the `v-else` template)
    - pnpm type-check passes
    - pnpm build passes
  </acceptance_criteria>
  <done>Servicios renders the canonical service list as a 1999 table.</done>
</task>

<task type="auto">
  <name>Task 3: QuienesSomos + Cliente + Contacto XS branches</name>
  <files>src/views/QuienesSomosView.vue, src/views/ClienteView.vue, src/views/ContactoView.vue</files>
  <read_first>
    - src/views/QuienesSomosView.vue, ClienteView.vue, ContactoView.vue (current — Phase 4 markup with .l-bold)
    - src/components/PortraitPlaceholder.vue (already has XS variant from Phase 1)
    - src/data/size-data.ts (team data, clientes data if present)
    - ExistingData/prototype-extracted/00021244_01_*, 00021325_07_*, 00021406_03_* (1999 spirit reference; explicit XS branches are not in the prototype — design within the vocabulary)
  </read_first>
  <action>
    For each of the 3 views, wrap the existing top-level template/section in a `<template v-else>` (or add `v-else` on the existing root section) and insert a sibling `v-if="style.code === 'xs'"` section above it. Same pattern used for Servicios.

    **QuienesSomosView XS:**
    ```vue
    <section v-if="style.code === 'xs'" class="qn-xs">
      <center>
        <h1 class="qn-xs-mark">SIZE</h1>
        <h2 class="qn-xs-h2">— EL EQUIPO —</h2>
      </center>
      <table cellpadding="8" cellspacing="0" class="qn-xs-team">
        <tr v-for="row in teamRows" :key="row.idx">
          <td v-for="member in row.members" :key="member.id" class="qn-xs-cell">
            <PortraitPlaceholder :id="member.id" />
            <br/><i>{{ member.name }}</i>
            <br/><small>{{ member.role }}</small>
          </td>
        </tr>
      </table>
      <hr/>
      <center><h2 class="qn-xs-h2">— CLIENTES —</h2></center>
      <table border="1" cellpadding="6" cellspacing="0" class="qn-xs-clients">
        <tr v-for="c in clientes" :key="c.id">
          <td>[ LOGO ]</td>
          <td><b>{{ c.name }}</b></td>
        </tr>
      </table>
    </section>
    ```
    `teamRows` is a computed that splits the existing team list (whatever shape `QuienesSomosView` already uses) into rows of 4 for the 4×5 grid. `clientes` is the existing client data; if not exposed, fall back to a hard-coded `[{id:'mma', name:'MMA El Valle'}, {id:'cranial', name:'Cranial Trading'}, {id:'sincero', name:'Sin-Cero'}]` array (these three are referenced in STATE.md "Pending Todos" / Phase 7 TBDs).

    **ClienteView XS:**
    ```vue
    <section v-if="style.code === 'xs'" class="cl-xs">
      <center><h1 class="cl-xs-mark">— CLIENTE —</h1></center>
      <div v-for="c in clientes" :key="c.id" class="cl-xs-block">
        <h3>{{ c.name }}</h3>
        <table border="1" cellpadding="6" cellspacing="0">
          <tr><th>Trabajo</th><th>Año</th></tr>
          <tr v-for="w in (c.works || [{name: 'Trabajo TBD', year: '—'}])" :key="w.name">
            <td>{{ w.name }}</td><td>{{ w.year }}</td>
          </tr>
        </table>
        <hr/>
      </div>
    </section>
    ```

    **ContactoView XS:**
    ```vue
    <section v-if="style.code === 'xs'" class="ct-xs">
      <center><h1 class="ct-xs-mark">— CONTACTO —</h1></center>
      <table cellpadding="12" cellspacing="0" class="ct-xs-grid">
        <tr>
          <td valign="top" class="ct-xs-channels">
            <h3>Canales</h3>
            <ul>
              <li><a href="https://instagram.com/size">Instagram</a></li>
              <li><a href="https://wa.me/5930000000">WhatsApp</a></li>
              <li><a href="mailto:hola@size.ec">hola@size.ec</a></li>
            </ul>
          </td>
          <td valign="top" class="ct-xs-bot">
            <h3>Chatbot</h3>
            <p>[ CHATBOT 1999 GOES HERE — TBD Phase 8 ]</p>
          </td>
        </tr>
      </table>
      <hr/>
      <center><h3>Escríbenos</h3></center>
      <form class="ct-xs-form">
        <table cellpadding="6" cellspacing="0" align="center">
          <tr><td align="right"><label for="ct-xs-nombre">Nombre:</label></td><td><input id="ct-xs-nombre" type="text" /></td></tr>
          <tr><td align="right"><label for="ct-xs-email">Email:</label></td><td><input id="ct-xs-email" type="email" /></td></tr>
          <tr><td align="right" valign="top"><label for="ct-xs-msg">Mensaje:</label></td><td><textarea id="ct-xs-msg" rows="4" cols="40"></textarea></td></tr>
          <tr><td></td><td><input type="submit" value="Enviar" /></td></tr>
        </table>
      </form>
    </section>
    ```
    The form `<input type="submit">` is a no-op for now (Phase 9 wires Firebase). The form attribute defaults to non-submitting because there's no `action` attribute and the submit handler is wired to nothing.

    **Scoped CSS for all three views — basic only:**
    - `font-family: "Times New Roman", serif`
    - `padding: 16px` on the section
    - Headings use `font-size` in px, no letter-spacing tricks beyond `letter-spacing: 4px` if matching the SIZE wordmark
    - Tables: `border-collapse: collapse`, `border: 1px solid #000` per `<th>`/`<td>` where applicable
    - Lists: default `<ul>` styling (no `list-style: none`)
    - Forms: `<input>`, `<textarea>` keep browser-default chrome (no `border-radius`, no custom `box-shadow`)

    No `:deep()`. No `display: flex`. No `display: grid`. No `transform`. No `transition`. No `border-radius`. No `box-shadow`.

    Existing M/S/L markup is wrapped in `<template v-else>` (or the root `<section>` gets a `v-else`). Phase 4's `.l-bold` rules stay inside the v-else block, untouched.

    Add a comment at the top of each XS section: `<!-- XS branch — Web 1999 literal: do NOT add Tailwind classes, flex, grid, or modern CSS effects here. -->`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/views/QuienesSomosView.vue','src/views/ClienteView.vue','src/views/ContactoView.vue']; for(const p of files){const s=fs.readFileSync(p,'utf8'); if(!/v-if=\"style\.code === 'xs'\"/.test(s)){console.error('missing v-if XS in',p);process.exit(1)} if(!/&lt;table/i.test(s)){console.error('missing table in',p);process.exit(1)} if(!/Times New Roman/.test(s)){console.error('missing Times New Roman in',p);process.exit(1)}} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 views has a `v-if="style.code === 'xs'"` section above the existing markup
    - Each XS section uses `<table>` for layout
    - Each XS section uses `Times New Roman` font
    - Existing M/S/L markup is now inside `v-else` and remains structurally unchanged
    - No `display: flex` / `display: grid` / `border-radius` / `box-shadow` / `transform` / `transition` in any `.qn-xs`, `.cl-xs`, `.ct-xs` scoped CSS rules
    - Comment at top of each XS section reminds the next reader not to modernize
    - pnpm type-check passes
    - pnpm build passes
  </acceptance_criteria>
  <done>The 4 protected views render 1999 layouts when XS is active.</done>
</task>

</tasks>

<verification>
- pnpm type-check exits 0
- pnpm build exits 0
- pnpm check:contrast still exits 0 with OVERALL 16/16 (token edits in 05-01 stand)
- Manual: pick XS from footer slider; Home shows ★ SIZE ★ + yellow marquee + native dropdown; Servicios shows the 11-service table; Quiénes shows the 4×5 photo grid + clientes table; Cliente shows per-cliente tables; Contacto shows the 2-col channels/bot grid + form table.
</verification>

<success_criteria>
ROADMAP §Phase 5 success criteria 1, 2, 5 (XS look, all 11 services in canonical order, locked DECISION-XS-RETRO) are visually delivered. Criterion 4 (WCAG AA) was closed in 05-01. Criterion 3 (IE11) is deferred per DEC-051.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tamano-xs-plain/05-02-SUMMARY.md`.
</output>
