---
phase: 04-tamanos-s-y-l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-contrast.cjs
  - src/styles/main.css
  - .planning/phases/04-tamanos-s-y-l/04-CONTRAST-RESULTS.md
autonomous: true
requirements: [REQ-sizes-five]
must_haves:
  truths:
    - "scripts/check-contrast.cjs parses html.level-s and html.level-l token blocks from main.css and runs the same 5-pair contract per level"
    - "All 15 themes (M + 12 markets + S + L) pass WCAG AA"
    - "If S or L fail, smallest-tweak remediation is applied to main.css; >1 token change per level surfaces to the user before commit (DEC-018 pattern)"
    - "Final pnpm check:contrast exits 0 with OVERALL 15/15"
  artifacts:
    - path: "scripts/check-contrast.cjs"
      provides: "Extended to S + L levels alongside M baseline + 12 markets"
      contains: "level-s|level-l"
    - path: ".planning/phases/04-tamanos-s-y-l/04-CONTRAST-RESULTS.md"
      provides: "Per-level audit with before/after if any token tuned"
      contains: "WCAG AA"
  key_links:
    - from: "pnpm check:contrast"
      to: "S + L token blocks in main.css"
      via: "regex extraction of html.level-s and html.level-l property values"
      pattern: "html\\.level-(s|l)"
---

<objective>
Extend the contrast checker to cover S and L tokens. Surface and remediate any sub-AA pairs. Phase 4 cannot ship S+L UI without WCAG AA proven on both intensities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-tamanos-s-y-l/04-CONTEXT.md
@scripts/check-contrast.cjs
@src/styles/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check-contrast.cjs to S and L</name>
  <files>scripts/check-contrast.cjs</files>
  <read_first>
    - scripts/check-contrast.cjs (current — M + 12 markets)
    - src/styles/main.css (html.level-s and html.level-l blocks — read the property values verbatim; do NOT inline copies)
    - .planning/phases/04-tamanos-s-y-l/04-CONTEXT.md (D-04: regex extraction)
  </read_first>
  <action>
    Extend the script to parse `src/styles/main.css` for two additional level blocks:

    ```css
    html.level-s {
      --bg: #FFFFFF;
      --ink: #1d1d1f;
      --muted: #6e6e73;
      ...
      --accent: #0066CC;
      ...
    }
    html.level-l {
      --bg: #FFEE00;
      --ink: #000000;
      --muted: #000000;
      ...
      --accent: #FF00AA;
      ...
    }
    ```

    Use a regex to extract `--bg`, `--ink`, `--muted`, `--accent` per block. For each level, build a token bag with `bg`, `ink`, `muted` (use the explicit `--muted` from the block, NOT the has-market derivation since these are level modes), `accent` (the `--accent` value). Run the same 5-pair contract used for M and markets. Note: L's `--muted: #000` and `--ink: #000` will pass body trivially (∞:1) but `accent inline` (FF00AA on FFEE00) and `accent CTA` (FFEE00 on FF00AA) might be tight — these are the pairs to watch.

    After all themes (M + 12 markets + S + L = 15), the OVERALL line reports `15/15` or names the failing themes. Exit non-zero if any fail.

    Add a new CLI flag `--levels-only` to print only M + S + L (skip markets). Keep `--markets-only` from Phase 3.

    Do NOT introduce any third-party dep. Pure Node.
  </action>
  <verify>
    <automated>node scripts/check-contrast.cjs 2>&1 | tee /tmp/contrast.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/contrast.txt','utf8'); if(!/SIZE — S\\b/.test(t)){console.error('missing S block');process.exit(1)} if(!/SIZE — L\\b/.test(t)){console.error('missing L block');process.exit(1)} if(!/OVERALL\\s+\\d+\\/15/.test(t)){console.error('OVERALL not /15');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Script prints S and L blocks alongside M and the 12 markets
    - OVERALL line reads `N/15`
    - `--levels-only` flag prints only M + S + L (3 blocks)
    - Token values are READ from main.css, not duplicated in the script
  </acceptance_criteria>
  <done>The contrast contract covers all the styles Phase 4 ships.</done>
</task>

<task type="auto">
  <name>Task 2: Triage S/L failures + remediate</name>
  <files>src/styles/main.css, .planning/phases/04-tamanos-s-y-l/04-CONTRAST-RESULTS.md</files>
  <read_first>
    - Output of pnpm check:contrast from Task 1
    - src/styles/main.css (html.level-s and html.level-l blocks)
    - .planning/phases/04-tamanos-s-y-l/04-CONTEXT.md (DECISION-LX-LOCKED — primary L palette is non-negotiable)
    - .planning/phases/03-mercados-sobre-m/03-CONTRAST-RESULTS.md (remediation pattern)
  </read_first>
  <action>
    For each failing pair in S or L:

    1. Identify which token to tweak. For L specifically: DECISION-LX-LOCKED locks `#FFEE00` bg, `#000` ink, `#FF00AA` accent. Don't change those three. If `accent CTA` (BG on PRIMARY = FFEE00 on FF00AA) fails, the visual fix is to deepen the magenta — but DECISION-LX-LOCKED says no. In that case, surface to the user as a palette conflict.
    2. For S: `#0066CC` accent on white BG. If `accent inline` (BG on PRIMARY = ffffff on 0066cc) tight, tweak the blue's lightness by 5-10 HSL-L units while preserving the brand identity.
    3. Apply minimum tweaks to `src/styles/main.css` only. Do NOT touch level-m (it's locked at 13/13 from Phase 3).
    4. Re-run pnpm check:contrast. If still failing, adjust until threshold met.
    5. If a level needs >1 token change, STOP and surface — DEC-018 pattern.

    Document the audit in `04-CONTRAST-RESULTS.md` mirroring 03-CONTRAST-RESULTS.md format:

    ```markdown
    # Phase 4 — S + L WCAG AA contrast audit

    Generated: <date>
    Source: pnpm check:contrast against src/styles/main.css level-s and level-l blocks

    ## S (Clean) baseline
    | pair | ratio | result |
    | body | 14.2:1 | PASS |
    ...

    ## L (Bold) baseline
    | pair | ratio | result |
    ...

    ## Tweaks applied
    | level | token | before | after | reason |
    ```

    Final state: pnpm check:contrast exits 0 with `OVERALL 15/15`.
  </action>
  <verify>
    <automated>pnpm check:contrast 2>&1 | tee /tmp/contrast.txt; node -e "const fs=require('fs'); const t=fs.readFileSync('/tmp/contrast.txt','utf8'); if(!/OVERALL\\s+15\\/15 themes pass/.test(t)){console.error('not all 15 pass:\\n',t);process.exit(1)} console.log('all 15 pass');"; node -e "const fs=require('fs'); const p='.planning/phases/04-tamanos-s-y-l/04-CONTRAST-RESULTS.md'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); for(const t of ['S (Clean)','L (Bold)']){if(!s.includes(t)){console.error('missing',t);process.exit(1)}} console.log('audit covers S and L');"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm check:contrast exits 0 with OVERALL 15/15
    - 04-CONTRAST-RESULTS.md exists with S and L sections
    - Any tweaks documented before/after
    - DECISION-LX-LOCKED tokens (#FFEE00, #000, #FF00AA) untouched (or surfaced if a fail forces a change)
    - pnpm type-check + pnpm build pass
  </acceptance_criteria>
  <done>S and L pass WCAG AA on the same 5-pair contract.</done>
</task>

</tasks>

<verification>
- pnpm check:contrast exits 0 with OVERALL 15/15
- pnpm type-check + pnpm build pass
- 04-CONTRAST-RESULTS.md captures the audit
</verification>

<success_criteria>
ROADMAP §Phase 4 success criterion #3 (WCAG AA legibility holds in both S and L across all 5 views) auto-verified at the token level. Per-component visual treatment is wired in 04-02; 04-03 captures the manual UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tamanos-s-y-l/04-01-SUMMARY.md`.
</output>
