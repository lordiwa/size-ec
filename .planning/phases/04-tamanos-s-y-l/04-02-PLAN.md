---
phase: 04-tamanos-s-y-l
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/LMarquee.vue
  - src/views/HomeView.vue
  - src/views/ServiciosView.vue
  - src/views/ClienteView.vue
  - src/views/ContactoView.vue
  - src/views/QuienesSomosView.vue
autonomous: true
requirements: [REQ-sizes-five]
must_haves:
  truths:
    - "HomeView renders distinct M / S / L layouts based on style.code, matching prototype/home.jsx verbatim"
    - "Servicios L cards rotate ±0.5° with 4px black border and 8px chunky shadow; alternate yellow / black / white bg every 4th card"
    - "Quiénes somos team photo borders become 4px black + 6px accent shadow in L; client cards alternate yellow/white in L with 8px shadow"
    - "Cliente individual work cards alternate yellow/white in L with 6px black shadow"
    - "Contacto channels card BG=#FFEE00 with 8px black shadow in L; chatbot card BG=#fff with accent shadow"
    - "LMarquee component renders only when style.code === 'l' on Home; uses existing l-marquee keyframes"
    - "S branches inherit the M markup but pick up level-s tokens (lighter bg, blue accent) automatically"
    - "prefers-reduced-motion still wins (existing global rule in main.css collapses animation-duration)"
  artifacts:
    - path: "src/components/LMarquee.vue"
      provides: "Brutalist horizontal marquee for L home (CSS-only, uses l-marquee keyframes)"
      contains: "PUBLICIDAD A TU MEDIDA"
    - path: "src/views/HomeView.vue"
      provides: "Per-level home layouts (M / S / L)"
      contains: "style.code === 'l'"
  key_links:
    - from: "HomeView L branch"
      to: "LMarquee component"
      via: "v-if='style.code === \\'l\\''"
      pattern: "&lt;LMarquee /&gt;"
    - from: "Servicios card class binding"
      to: "L brutalist treatment"
      via: ":class binding on card wrapper"
      pattern: "l-bold"
---

<objective>
Ship the prototype's S and L visual treatments across the 5 views by wiring per-level branches in HomeView and per-card class bindings in the other 4 protected views. Match `ExistingData/prototype-extracted/*.js` verbatim — no inventing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-tamanos-s-y-l/04-CONTEXT.md
@.planning/phases/04-tamanos-s-y-l/04-01-SUMMARY.md
@ExistingData/prototype-extracted/00021082_04_a353c98e-7080-4c7c-859d-e8ebafc9597d.js
@ExistingData/prototype-extracted/00021163_02_c6135f0c-28a8-4eb5-9e32-e33aa46e7bc4.js
@ExistingData/prototype-extracted/00021244_01_86c85a12-94ee-473b-b21e-d8227f98510a.js
@ExistingData/prototype-extracted/00021325_07_6e363be8-180c-48b7-a8e2-f91c5b740b91.js
@ExistingData/prototype-extracted/00021406_03_7e82b5d6-c24b-4074-9bfa-0169e271158c.js
@src/views/HomeView.vue
@src/views/ServiciosView.vue
@src/views/QuienesSomosView.vue
@src/views/ClienteView.vue
@src/views/ContactoView.vue
@src/components/MarketSelect.vue
@src/styles/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author LMarquee component</name>
  <files>src/components/LMarquee.vue</files>
  <read_first>
    - ExistingData/prototype-extracted/00021082_04_*.js (L home branch lines: marquee with 8 repeated "PUBLICIDAD A TU MEDIDA ★")
    - src/styles/main.css (already has @keyframes l-marquee from Phase 1 port; confirm it's still there)
  </read_first>
  <action>
    Create `src/components/LMarquee.vue`. The component takes no props; renders a horizontal flex of 8 `<span>PUBLICIDAD A TU MEDIDA ★ </span>` elements wrapped in a clipping container with `border-top: 4px solid #000`, `border-bottom: 4px solid #000`, `padding: 8px 0`, `font-family: 'Archivo Black', sans-serif`, `font-size: 24px`, `text-transform: uppercase`. Apply the existing `l-marquee` keyframes via `animation: l-marquee 30s linear infinite` on the inner row.

    Use scoped CSS only. Do not duplicate the keyframes — they live in main.css.

    `prefers-reduced-motion` is honored by the global rule.

    Component should also expose a `class="l-marquee"` to allow main.css's existing `html.level-l .l-l-marquee` rule to apply if any global tweaks land.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='src/components/LMarquee.vue'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); const checks=[/&lt;script setup/, /PUBLICIDAD A TU MEDIDA/, /animation:\\s*l-marquee/, /Archivo Black/, /4px solid/]; for(const c of checks){if(!c.test(s)){console.error('missing',c);process.exit(1)}} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `<script setup lang="ts">`
    - Renders ≥8 "PUBLICIDAD A TU MEDIDA" tokens
    - Uses `animation: l-marquee 30s linear infinite`
    - 4px black borders top + bottom
    - Archivo Black font
  </acceptance_criteria>
  <done>L brutalist marquee ready for Home.</done>
</task>

<task type="auto">
  <name>Task 2: Add S and L branches to HomeView</name>
  <files>src/views/HomeView.vue</files>
  <read_first>
    - ExistingData/prototype-extracted/00021082_04_*.js (HomeView function, L branch lines ~40-60, S branch lines ~78-89)
    - src/views/HomeView.vue (current — only M layout)
    - src/components/MarketSelect.vue (already accepts code prop for L chrome)
    - src/components/RotatingWord.vue (reuse across all branches)
  </read_first>
  <action>
    Refactor HomeView.vue to render per-level layouts. Keep one component; branch on `style.code`. Order: L → XL placeholder → S → XS placeholder → M (default).

    For Phase 4 only L and S branches matter (XL/XS render as M for now; their phases own them). The structure:

    ```vue
    <template>
      <section v-if="style.code === 'l'" class="home-l">
        <LMarquee />
        <h1 class="size-wordmark huge home-l-mark">SIZE</h1>
        <div class="home-l-grid">
          <div class="home-l-tag">
            Publicidad <span class="home-l-tag-accent">a tu medida.</span>
          </div>
          <div class="home-l-rotator-block">
            <div class="home-l-rotator">
              Somos tu <RotatingWord :words="words" :idx="wIdx" />.
            </div>
            <div class="home-l-cta-block">
              <MarketSelect :code="style.code" :value="style.marketId" @pick="pickMarket" />
            </div>
          </div>
        </div>
      </section>

      <section v-else-if="style.code === 's'" class="home-s">
        <h1 class="size-wordmark huge home-s-mark">SIZE</h1>
        <p class="home-s-promise">Publicidad a tu medida.</p>
        <p class="home-s-rotator">
          Somos tu <span class="home-s-accent"><RotatingWord :words="words" :idx="wIdx" /></span>.
        </p>
        <MarketSelect :code="style.code" :value="style.marketId" @pick="pickMarket" />
      </section>

      <section v-else class="home"> ...M layout already exists... </section>
    </template>
    ```

    Per the prototype:
    - **L** layout: marquee top + huge SIZE + 2-col grid (1fr 1fr). Left column: `Publicidad <accent inline yellow-on-black> a tu medida.</accent>` clamp(36px,5vw,72px) Archivo Black. Right column: huge `Somos tu [rotator]` clamp(40px,6vw,84px) magenta `#FF00AA`, then dropdown below. Padding `40px 6vw`, min-height 100vh.
    - **S** layout: centered. SIZE huge clamp(80px,16vw,220px). Tagline 24px muted. Rotator clamp(28px,4vw,52px) bold w/ accent on rotating word. Dropdown below centered. Padding `10vh 6vw`, min-height 100vh, text-align center.

    All scoped CSS lives in HomeView. Reuse `home-rotator-word`, `home-tag-accent` only for the M block — L and S have their own tokens.

    Do NOT delete the M layout; keep as `v-else`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/views/HomeView.vue','utf8'); const checks=[/import LMarquee/, /style\\.code === ['\"]l['\"]/, /style\\.code === ['\"]s['\"]/, /class=\"home-l\"/, /class=\"home-s\"/, /&lt;LMarquee/, /home-l-tag/, /home-s-promise/]; for(const c of checks){if(!c.test(s)){console.error('missing',c);process.exit(1)}} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - HomeView imports LMarquee and renders it inside the L branch
    - L branch present with 2-col grid (Publicidad / Somos tu) + dropdown
    - S branch present, centered, with SIZE huge + tagline + rotator + dropdown
    - M branch unchanged
    - pnpm type-check passes
    - pnpm build passes
  </acceptance_criteria>
  <done>Home renders three distinct layouts based on level.</done>
</task>

<task type="auto">
  <name>Task 3: Add L card treatments to Servicios + Cliente + Contacto + Quiénes</name>
  <files>src/views/ServiciosView.vue, src/views/ClienteView.vue, src/views/ContactoView.vue, src/views/QuienesSomosView.vue</files>
  <read_first>
    - ExistingData/prototype-extracted/00021163_02_*.js (servicios L card pattern: rotation, alternating bg, 4px borders, 8px shadow)
    - ExistingData/prototype-extracted/00021244_01_*.js (quienes L photo borders + L client cards)
    - ExistingData/prototype-extracted/00021325_07_*.js (cliente L work cards)
    - ExistingData/prototype-extracted/00021406_03_*.js (contacto L channels + chatbot)
    - src/stores/style.ts (style.code reactive)
  </read_first>
  <action>
    For each of the 4 views, add a class binding on the card wrapper that applies a `.l-bold` modifier when `style.code === 'l'`. Then add scoped CSS rules under `.l-bold ...` selectors that match the prototype's brutalist treatment.

    **ServiciosView (no-market and market branches):**
    - Card wrapper: `:class="{ 'l-bold': style.code === 'l' }"`
    - `.srv-card.l-bold:nth-child(4n+1)` → bg `var(--accent)` (magenta), color #000
    - `.srv-card.l-bold:nth-child(4n+3)` → bg #000, color `var(--accent)`
    - `.srv-card.l-bold:nth-child(4n+2), .srv-card.l-bold:nth-child(4n)` → bg #fff, color #000
    - `.srv-card.l-bold` → border `4px solid #000`, box-shadow `8px 8px 0 #000`, transform `rotate(...)` per card index using `:style="{ transform: \`rotate(${(i % 3 - 1) * 0.5}deg)\` }"`
    - Same pattern for `.srv-mkt-card.l-bold` but using `var(--mkt-primary)` / `var(--mkt-secondary)` / `#fff` and shadow `var(--mkt-ink)`.

    **QuienesSomosView:**
    - `.qn-photo` add `.l-bold` class binding; `.qn-photo.l-bold` → border `4px solid #000`, box-shadow `6px 6px 0 var(--accent)`.
    - `.qn-client` add `.l-bold` and alternate bg; `.qn-client.l-bold:nth-child(2n+1)` → bg `#FFEE00`; even → bg `#fff`. All `.qn-client.l-bold` → color #000, border `1px solid #000`, box-shadow `8px 8px 0 #000`, transform unchanged.

    **ClienteView:**
    - `.cl-work-card.l-bold:nth-child(odd)` bg `#FFEE00`, even bg `#fff`. All `.cl-work-card.l-bold` → color #000, border `1px solid var(--line)`, box-shadow `6px 6px 0 #000`.

    **ContactoView:**
    - `.ct-channels.l-bold` → bg `#FFEE00`, color #000, box-shadow `8px 8px 0 #000`.
    - `.ct-bot.l-bold` → bg `#fff`, color #000, box-shadow `8px 8px 0 var(--accent)`.

    All bindings use `style.code === 'l'`. The base styling for non-L levels (M/S) stays as it is — S inherits the existing card markup but with the level-s tokens swapped in via main.css.

    No global selectors needed; per-component scoped CSS only. Do not introduce `:deep()`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/views/ServiciosView.vue','src/views/QuienesSomosView.vue','src/views/ClienteView.vue','src/views/ContactoView.vue']; for(const p of files){const s=fs.readFileSync(p,'utf8'); if(!/style\\.code === ['\"]l['\"]/.test(s) && !/'l-bold'\\s*:\\s*style\\.code === ['\"]l['\"]/.test(s)){console.error('missing l-bold class binding in',p);process.exit(1)} if(!/\\.l-bold/.test(s)){console.error('missing .l-bold style in',p);process.exit(1)}} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 views has `:class="{ 'l-bold': style.code === 'l' }"` on at least one wrapper
    - Each view has at least one scoped `.l-bold ...` CSS rule
    - Servicios has both no-market and market L treatments
    - Quiénes photos and clients both wired
    - Cliente work cards wired
    - Contacto channels and chatbot both wired
    - pnpm type-check passes
    - pnpm build passes
  </acceptance_criteria>
  <done>The 5 views (Home + the 4 protected) all carry the prototype's S and L treatments.</done>
</task>

</tasks>

<verification>
- pnpm type-check exits 0
- pnpm build exits 0
- pnpm check:contrast exits 0 with OVERALL 15/15 (still — token edits in 04-01 stand)
- Manual: pick L from footer slider; Home shows marquee + 2-col grid + magenta accents; click Servicios → cards rotate, alternating yellow/black/white. Pick S; Home centers; cards lose the rotation and adopt clean white treatment.
</verification>

<success_criteria>
ROADMAP §Phase 4 success criteria 1, 2, 5 (S clean / L brutalist / 60-combination invariant) are visually delivered. Criterion 3 (WCAG AA) was closed in 04-01. Criterion 4 (~600ms transition) is covered by the existing main.css transition rule.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tamanos-s-y-l/04-02-SUMMARY.md`.
</output>
