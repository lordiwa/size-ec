---
phase: 04-tamanos-s-y-l
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - .planning/phases/04-tamanos-s-y-l/04-UAT.md
autonomous: true
requirements: [REQ-sizes-five]
must_haves:
  truths:
    - "04-UAT.md captures a 5×3 view-state matrix (5 views × 3 sizes S/M/L) with PASS/FAIL per cell"
    - "Reduced-motion sweep documented as a single sign-off line — verifies the L marquee + L card hovers freeze under prefers-reduced-motion"
    - "Transition smoke documented — switching M → S → L → M each completes in ~600ms with the Reconfigurando overlay covering the gap"
    - "References 04-CONTRAST-RESULTS.md so reviewer confirms WCAG AA before walking the matrix"
  artifacts:
    - path: ".planning/phases/04-tamanos-s-y-l/04-UAT.md"
      provides: "Per-(view, level) sign-off matrix for S + L on top of M baseline"
      contains: "L (Bold)"
  key_links:
    - from: "04-UAT.md"
      to: "04-CONTRAST-RESULTS.md"
      via: "explicit reference at top"
      pattern: "04-CONTRAST-RESULTS"
---

<objective>
Phase 4 ships visual treatments that demand a human eye. This plan produces the operator's sign-off doc — Phase 4 isn't done until the matrix is ticked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-tamanos-s-y-l/04-CONTEXT.md
@.planning/phases/04-tamanos-s-y-l/04-01-SUMMARY.md
@.planning/phases/04-tamanos-s-y-l/04-02-SUMMARY.md
@.planning/phases/03-mercados-sobre-m/03-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 04-UAT.md sign-off matrix</name>
  <files>.planning/phases/04-tamanos-s-y-l/04-UAT.md</files>
  <read_first>
    - .planning/phases/03-mercados-sobre-m/03-UAT.md (mirror format)
    - .planning/phases/04-tamanos-s-y-l/04-CONTEXT.md (D-06: 5×3=15 cells + reduced-motion + transition smoke + sign-off)
    - .planning/phases/04-tamanos-s-y-l/04-CONTRAST-RESULTS.md (referenced from this UAT)
  </read_first>
  <action>
    Create `04-UAT.md` with sections:

    **Header** — references `04-CONTRAST-RESULTS.md` for the auto-verified WCAG AA contract.

    **A. 5×3 view-state matrix** — checkbox grid (operator opens `pnpm dev`, picks each level, walks each view):

    ```
                        S       M       L
    Home                [ ]     [ ]     [ ]
    Servicios           [ ]     [ ]     [ ]
    Quiénes somos       [ ]     [ ]     [ ]
    Cliente             [ ]     [ ]     [ ]
    Contacto            [ ]     [ ]     [ ]
    ```

    Each cell: confirms the view renders without console errors, the level personality reads (S = clean white + blue accent + sans; M = dark editorial; L = brutalist yellow + magenta accent + Archivo Black), the Reconfigurando overlay played on entry, and WCAG AA holds (already auto-confirmed for tokens; this is a visual sanity check).

    **B. Per-level highlights** — ask the operator to specifically confirm:
    - L Home marquee scrolls smoothly across the top
    - L Servicios cards rotate ±0.5° with chunky shadows + alternating yellow/black/white
    - L Quiénes photo borders are 4px black with 6px magenta shadow
    - L Cliente work cards alternate yellow/white
    - L Contacto channels card is yellow with 8px black shadow
    - S Home centers everything; tagline + rotator are clean
    - S Servicios cards are flat (no rotation, no chunky shadows)

    Each as a [ ] checkbox.

    **C. Reduced-motion sweep** — single line:
    > [ ] Toggle OS-level reduced motion. The L marquee freezes mid-scroll. L card hovers no longer translate. Reconfigurando overlay still flashes (it's status, not motion). Rotator on Home freezes.

    **D. Transition smoke** — single line:
    > [ ] Pick M → S → L → M from the footer mini-slider. Each transition: ~600ms body bg/ink fade, Reconfigurando overlay covers ~900ms, then the new layout commits. No full reload, no flash beyond the overlay.

    **E. Sign-off table** — Reviewer / Date / All cells PASS? / Notes.

    Total ≥21 checkboxes (15 matrix + 5 highlights + 1 reduced-motion + 1 transition smoke).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/04-tamanos-s-y-l/04-UAT.md'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); const required=['04-CONTRAST-RESULTS','5×3','Home','Servicios','Quiénes','Cliente','Contacto','Reconfigurando','prefers-reduced-motion','marquee','Archivo Black']; for(const k of required){if(!s.toLowerCase().includes(k.toLowerCase())){console.error('missing',k);process.exit(1)}} const boxes=(s.match(/\\[ \\]/g)||[]).length; if(boxes<21){console.error('expected ≥21 checkboxes, got',boxes);process.exit(1)} console.log('ok',boxes,'boxes');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all required sections
    - Mentions all 5 view names + S, M, L
    - References 04-CONTRAST-RESULTS.md
    - At least 21 checkboxes
    - Sign-off table at the bottom
  </acceptance_criteria>
  <done>Phase 4 has a single document the operator signs off before Phase 5 (XS) planning.</done>
</task>

</tasks>

<verification>
- 04-UAT.md exists with ≥21 checkboxes
- All 5 view names + S/M/L appear
- References 04-CONTRAST-RESULTS.md
</verification>

<success_criteria>
Operator can sign off Phase 4 in one walk: WCAG AA already auto-verified, S+L treatments visually confirmed across the 5 views, reduced-motion + transition behaviour confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tamanos-s-y-l/04-03-SUMMARY.md`.
</output>
