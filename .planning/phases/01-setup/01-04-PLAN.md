---
phase: 01-setup
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified: []
autonomous: false
requirements: []
user_setup:
  - service: firebase
    why: "First-time deploy requires the operator to authenticate firebase-tools against their Google account that owns the size-ec Firebase project"
    dashboard_config:
      - task: "Create the Firebase project with id 'size-ec' (or reconcile .firebaserc to the actual chosen id) on the Spark plan"
        location: "https://console.firebase.google.com/ — Add project → name 'size-ec' (or chosen name)"
      - task: "Enable Hosting on the project"
        location: "Firebase Console → Build → Hosting → Get started"
  - service: atlassian
    why: "Plan 04 needs the Atlassian API token in the live shell environment so the GSD MCP server can read/write Jira"
    env_vars:
      - name: ATLASSIAN_API_TOKEN
        source: "https://id.atlassian.com/manage-profile/security/api-tokens — see CLAUDE.md § Atlassian API token setup"
must_haves:
  truths:
    - "Invoking the deploy skill ships the current Vue 3 shell to Firebase Hosting"
    - "The site is reachable at https://<firebase-project-id>.web.app and returns the SIZE shell HTML"
    - "GSD MCP can list at least one issue (or report 0 issues + reachable) for project SIZE — proves the wiring works"
    - "Nine epics exist in Jira project SIZE, one per remaining roadmap phase (Phase 2 → Phase 10), each with the phase Goal as its description"
  artifacts:
    - path: "https://<firebase-project-id>.web.app"
      provides: "Reachable Firebase Hosting URL serving SIZE shell"
      contains: "(HTTP 200, SIZE wordmark or scaffold body present)"
    - path: "Jira project SIZE on cranialtrading.atlassian.net"
      provides: "9 seeded epics — one per Phase 2..10"
      contains: "epic titles matching ROADMAP.md phase names"
  key_links:
    - from: "deploy skill invocation"
      to: "Firebase Hosting"
      via: "pnpm exec firebase deploy --only hosting"
      pattern: "Hosting URL: https://.+\\.web\\.app"
    - from: "GSD MCP"
      to: "Atlassian Jira project SIZE"
      via: ".mcp.json + ATLASSIAN_API_TOKEN env var"
      pattern: "project SIZE issues listed (or empty-but-reachable)"
---

<objective>
Execute the first real smoke deploy via the deploy skill, verify the Firebase URL is reachable, verify GSD MCP is wired to SIZE Jira, and seed the nine roadmap epics.

Purpose: Closes Phase 1 success criteria #1 (push-to-deploy reachable at Firebase default URL) and #2 (GSD MCP reachable + 9 epics seeded). This plan is non-autonomous because Firebase login is interactive on first use, the Atlassian project must exist on the dashboard side, and the user must export the API token in their shell.
Output: A live Firebase Hosting URL serving the SIZE shell; GSD MCP returns a successful Jira query; 9 epics created in project SIZE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup/01-CONTEXT.md
@.planning/phases/01-setup/01-01-SUMMARY.md
@.planning/phases/01-setup/01-02-SUMMARY.md
@.planning/phases/01-setup/01-03-SUMMARY.md
@CLAUDE.md
@firebase.json
@.firebaserc
@.mcp.json
@.claude/skills/deploy/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human prerequisites — Firebase project + Atlassian token in env</name>
  <what-built>
    Plans 01–03 produced: firebase.json + .firebaserc bound to project id `size-ec`, .mcp.json with `${env:ATLASSIAN_API_TOKEN}` interpolation, CLAUDE.md with the Atlassian token setup section, and the deploy skill at `.claude/skills/deploy/SKILL.md`.

    Before the first smoke deploy, two things require the human (Claude cannot do these — Firebase project creation needs the Google-account-owner UI, and exporting an env var must happen in the user's actual shell, not a Claude Code subshell).
  </what-built>
  <how-to-verify>
    Confirm each of the following has been done in your environment:

    1. **Firebase project exists.** Visit https://console.firebase.google.com/ and confirm a project with id `size-ec` exists on the Spark plan with Hosting enabled. If you used a different id, edit `.firebaserc` so `projects.default` matches your chosen id (and tell Claude the new id when resuming).
    2. **firebase-tools is logged in.** In your shell, run `pnpm exec firebase login` once; complete the browser auth flow if prompted. Then `pnpm exec firebase projects:list` should print your project. (One-time per machine.)
    3. **Atlassian API token is in your environment.** Per CLAUDE.md § Atlassian API token setup: generate a token at https://id.atlassian.com/manage-profile/security/api-tokens and export `ATLASSIAN_API_TOKEN` in this shell. Verify with PowerShell `[bool]$env:ATLASSIAN_API_TOKEN` (must be True) or POSIX `[ -n "$ATLASSIAN_API_TOKEN" ] && echo set`.
    4. **Atlassian email matches.** Confirm the Atlassian account that owns the token uses the email already in `.mcp.json` (`srparca@gmail.com`). If the owning email is different, tell Claude the correct email when resuming so `.mcp.json` can be reconciled.

    Do NOT paste the token here. Type "ready" once all four items are confirmed.
  </how-to-verify>
  <resume-signal>Type "ready" (or describe blockers — e.g., "different project id is size-ecuador" or "token email is foo@bar.com")</resume-signal>
  <read_first>
    - .planning/phases/01-setup/01-CONTEXT.md (placeholder domain decision: Firebase default `<project-id>.web.app`)
    - CLAUDE.md (§ Atlassian API token setup — must be present from Plan 02)
    - .firebaserc (current default project id)
  </read_first>
  <acceptance_criteria>
    - User responds "ready" (or provides reconciliations Claude applies before continuing)
    - If reconciliations were provided: `.firebaserc` and/or `.mcp.json` updated accordingly and the diff is shown to the user
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the deploy skill for the first smoke deploy</name>
  <files>(no source changes — this task creates a deploy artifact on Firebase Hosting and may produce a deploy commit)</files>
  <read_first>
    - .claude/skills/deploy/SKILL.md (the chain to follow — order is non-negotiable)
    - .firebaserc (the project id that determines the deploy target)
    - .planning/phases/01-setup/01-03-SUMMARY.md (confirms the skill landed)
  </read_first>
  <action>
    Invoke the deploy skill. Pass the commit message: `chore(01): first smoke deploy via deploy skill`.

    Execute the chain exactly as documented in `.claude/skills/deploy/SKILL.md`:
    1. `git add -A`
    2. `git commit -m "chore(01): first smoke deploy via deploy skill"` (if "nothing to commit", continue per skill rules)
    3. `git push origin main` (use `-u` if upstream not set)
    4. `pnpm type-check` — must pass; abort otherwise
    5. `pnpm build` — must pass; abort otherwise
    6. `pnpm exec firebase deploy --only hosting`

    Capture the hosting URL printed at the end. It will be of the form `https://<project-id>.web.app` (and likely also `https://<project-id>.firebaseapp.com`). Record it for Task 3 verification.

    If `firebase deploy` errors with `not authenticated` or `Failed to authenticate`, surface the message to the user and request they re-run the human prereq step (`pnpm exec firebase login`). Do NOT retry indefinitely.

    If `firebase deploy` errors with `Project not found` or similar, surface the actual project id that .firebaserc points to and ask the user to either create that project in the Firebase Console or update `.firebaserc` to match an existing project id, then resume.
  </action>
  <verify>
    <automated>node -e "const {execSync}=require('child_process'); const r=require('./.firebaserc'); const id=r.projects.default; const url='https://'+id+'.web.app'; console.log('checking',url); const https=require('https'); https.get(url,(res)=>{console.log('status',res.statusCode); if(res.statusCode>=200&&res.statusCode<400){let data=''; res.on('data',c=>data+=c); res.on('end',()=>{if(data.length<100){console.error('body too small',data.length);process.exit(2)} console.log('body bytes',data.length); process.exit(0)})} else {console.error('non-2xx/3xx',res.statusCode);process.exit(1)}}).on('error',e=>{console.error('err',e.message);process.exit(3)});"</automated>
  </verify>
  <acceptance_criteria>
    - `firebase deploy --only hosting` exits 0
    - The deploy output prints a `Hosting URL: https://<id>.web.app` line
    - `https://<id>.web.app` returns HTTP 200 with a non-trivial body (> 100 bytes)
    - Local working tree is either clean or contains only the post-deploy `.firebase/` cache (which is gitignored from Plan 01)
  </acceptance_criteria>
  <done>SIZE shell is live on Firebase Hosting at the default URL; Phase 1 success criterion #1 is satisfied (modulo Cloudflare proxy, which is explicitly deferred per CONTEXT.md)</done>
</task>

<task type="auto">
  <name>Task 3: Verify GSD MCP is wired to SIZE Jira (project SIZE reachable)</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - .mcp.json (the MCP server registration from Plan 02)
    - CLAUDE.md (§ Atlassian API token setup)
    - .planning/phases/01-setup/01-CONTEXT.md (Jira board reference: cranialtrading.atlassian.net board 100)
  </read_first>
  <action>
    Confirm the GSD MCP server is reachable from this Claude Code session and authenticated against project SIZE.

    Concrete check: ask the GSD MCP server to list issues (or epics) for project key `SIZE`. Acceptable outcomes:
    - Returns one or more issues → wiring works.
    - Returns zero issues but with a clean response (project found, no issues yet) → wiring works (Phase 1 has not seeded anything yet).
    - Returns auth error / project-not-found / network error → wiring is broken; surface the exact error and stop.

    If the MCP server is not registered in this session, instruct the user to restart Claude Code in this checkout so `.mcp.json` is picked up, then retry.

    If the auth check fails, the most likely cause is the env var is unset in the Claude Code parent process. Surface that explicitly: `"ATLASSIAN_API_TOKEN" appears unset in the MCP server's environment — confirm it is exported in the shell that launched Claude Code, then restart Claude Code`.

    Do NOT print the token. Do NOT log the token. Confirm by listing project SIZE only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const j=require('./.mcp.json'); if(!j.mcpServers||!j.mcpServers.gsd){console.error('mcp not registered');process.exit(1)} if(j.mcpServers.gsd.env.ATLASSIAN_API_TOKEN!=='\${env:ATLASSIAN_API_TOKEN}'){console.error('token interpolation mismatch');process.exit(2)} console.log('mcp config ok — runtime reachability is human-confirmed');"</automated>
  </verify>
  <acceptance_criteria>
    - GSD MCP server is registered in this Claude Code session (visible to the assistant as an available MCP tool)
    - A list-issues / list-epics call against project SIZE returns successfully (any non-error response)
    - If a sample issue is returned, its `project.key` (or equivalent) equals `SIZE`
    - The verification was done WITHOUT printing the token to stdout, logs, or chat
  </acceptance_criteria>
  <done>GSD MCP is confirmed wired to SIZE Jira; Phase 1 success criterion #2 (reachability half) is satisfied; ready to seed epics in Task 4</done>
</task>

<task type="auto">
  <name>Task 4: Seed nine Jira epics for Phases 2–10 via GSD MCP</name>
  <files>(no file changes — Jira mutation only)</files>
  <read_first>
    - .planning/ROADMAP.md (lines 30–158: Phase 2 through Phase 10 — phase names + Goal sentences are the epic title + description sources)
    - .planning/phases/01-setup/01-CONTEXT.md (specifics: epic title = phase name; description = phase Goal sentence)
  </read_first>
  <action>
    Using the GSD MCP server confirmed in Task 3, create exactly nine epics in Jira project `SIZE`. For each epic, pull the title and description directly from ROADMAP.md (do not paraphrase):

    | # | Title (verbatim from ROADMAP heading) | Description (verbatim from ROADMAP "Goal:" sentence) |
    |---|---|---|
    | 1 | `Phase 2: Sistema base` | The styling and routing skeleton works end-to-end. A user can navigate the 5 routes, see a sticky footer, hit the creativity gate on first visit, pick a size or a market, watch the site re-style in place via a single Pinia store backed by one localStorage flag, and see Home's rotating word animate accessibly. |
    | 2 | `Phase 3: Tamaño M (Crafted) y default visual` | M (Crafted) is the working default everywhere. A first-time or recurring visitor with no flag lands on every view in M dark-mode, reads the 11 services, sees the team in M, and reaches Contacto in M — with WCAG AA verified and ~600ms transitions. |
    | 3 | `Phase 4: Mercados sobre M` | All 12 market styles work on top of the M baseline. Each market is immediately recognisable as its industry, filters Servicios to its canonical service subset, and stays WCAG AA legible. The 12 × 4 = 48-state QA matrix passes. |
    | 4 | `Phase 5: Tamaños S y L` | S (Clean) and L (Bold) ship as neutral SIZE expressions. S is corporate clean and flat with zero animation JS; L is contemporary brutalist with full GSAP + Lottie, lazy-loaded, still WCAG AA. |
    | 5 | `Phase 6: Tamaño XS (Plain)` | XS / Plain is the literal Web 1999 size — gris, Times New Roman, links azules — and is genuinely usable, with the size catalog now complete except for XL. |
    | 6 | `Phase 7: Tamaño XL (Unleashed)` | XL / Unleashed is fully alive — Three.js / Phaser / Tone.js / postprocessing / physics — but it always degrades cleanly to L when WebGL2 is missing, and progressive loading verifies that XS-M ship lightweight bundles end-to-end. |
    | 7 | `Phase 8: Contenido` | The site is content-complete. The Equipo grid has 20 photos and 20 comments (4 founders × 5 sizes), three full client cases ship at their slugs, and the Home rotating-words list is final. |
    | 8 | `Phase 9: Integraciones` | Contacto goes from static placeholder to live: Firestore stores submissions, transactional email notifies the team, the chosen chatbot provider replaces the placeholder, and the corporate email channel is real on the left card. |
    | 9 | `Phase 10: Cierre` | The site is launch-ready. SEO + analytics + final QA across the 17 styles + cutover to the definitive domain. |

    For each: invoke the GSD MCP create-issue tool with `project=SIZE`, `issuetype=Epic`, `summary=<title>`, `description=<goal sentence>`. If the GSD MCP exposes a more specific create-epic tool, use that. Capture the returned issue key (e.g., `SIZE-123`) for each epic.

    **Idempotency guard:** Before creating, list existing epics in project SIZE. If an epic with the exact title already exists, skip creation for that row and reuse the existing key. This prevents duplicate epics if Plan 04 is re-run.

    **Order:** Create them in numeric order (Phase 2 first, Phase 10 last) so the Jira board reads in roadmap sequence.

    After all nine are created (or reconciled), produce a summary table for the user: title → issue key.

    Do NOT add any other fields (no labels, no fixVersion, no assignee). Phase 1's job is creating the placeholders; subsequent phases will refine them.
  </action>
  <verify>
    <automated>echo "Verification of Jira state requires the MCP server response captured at runtime. Acceptance is judged by the response: 9 epic issue keys for project SIZE returned (or reconciled to existing keys with matching titles)."</automated>
  </verify>
  <acceptance_criteria>
    - Nine epics exist in project SIZE with exact titles matching the table above (the GSD MCP list-epics call returns all nine)
    - Each epic's description (or first line of description, depending on Jira renderer) starts with the Goal sentence as quoted from ROADMAP.md
    - Issue type for each is `Epic`
    - The summary table (title → issue key) is shown to the user
    - No duplicate epics were created (idempotency guard worked)
  </acceptance_criteria>
  <done>Phase 1 success criterion #2 fully satisfied — GSD MCP reachable AND nine epics seeded for Phases 2–10. Phase 1 deliverables complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Final Phase 1 verification — operator visual confirmation</name>
  <what-built>
    All four Phase 1 success criteria should now be satisfied:
    1. Push-to-deploy via the deploy skill landed the SIZE shell on Firebase Hosting at the default URL.
    2. GSD MCP is wired to project SIZE on cranialtrading.atlassian.net (board 100), and nine epics for Phase 2–10 exist.
    3. Local build (vite + TS, Tailwind + Pinia + Vue Router) is green; the scaffold renders Hello SIZE.
    4. CLAUDE.md carries the Definition-of-Done note referencing CON-013 plus the Atlassian token setup section.
  </what-built>
  <how-to-verify>
    1. **Firebase URL.** Open `https://<project-id>.web.app` (the URL printed by Task 2) in a browser. The SIZE scaffold should render — wordmark, brand promise, basic layout. Confirm no console errors block the first paint.
    2. **Jira board.** Open https://cranialtrading.atlassian.net/jira/software/projects/SIZE/boards/100. Confirm nine epics titled `Phase 2: Sistema base` through `Phase 10: Cierre` are visible (likely in Backlog).
    3. **CLAUDE.md.** Open `CLAUDE.md` in the editor. Confirm the `## Definition of Done` section references `CON-013` and the `## Atlassian API token setup` section is present.
    4. **Deploy skill (idempotent re-invoke, optional).** If you want to confirm the chain still works post-Phase-1, invoke the deploy skill with a no-op commit message — should land cleanly with "nothing to commit" but a successful redeploy.

    Type "approved" if all four pass. Otherwise describe the specific failure (which step, what you saw, what you expected) — the executor will course-correct.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 success criteria #1–#4 — exact text the verification maps to)
    - .planning/phases/01-setup/01-CONTEXT.md (placeholder domain decision — Firebase default URL is the acceptance bar, not Cloudflare)
  </read_first>
  <acceptance_criteria>
    - User responds "approved"
    - If issues are reported: a follow-up plan is drafted (gap closure) before Phase 1 is marked complete
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Firebase Hosting URL responds 200 with a non-trivial body
- Jira project SIZE has 9 epics matching ROADMAP.md phase names
- CLAUDE.md DoD + Atlassian sections are present (sanity-checked again)
- The deploy skill is the only deploy mechanism (no GitHub Actions workflow files were created)
</verification>

<success_criteria>
All four Phase 1 success criteria from ROADMAP.md are TRUE in production-equivalent state:
1. Push-to-deploy works via the project-local skill; site reachable at Firebase default URL.
2. GSD MCP reachable; 9 epics seeded.
3. Local build green; Hello SIZE renders.
4. CLAUDE.md DoD note references CON-013; Atlassian token setup documented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup/01-04-SUMMARY.md`. Include the live Firebase URL, the 9 epic issue keys, and any reconciliations made during Task 1 (project id, email).
</output>
