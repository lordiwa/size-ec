---
phase: 09-cierre-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .mcp.json
autonomous: true
requirements: []
user_setup:
  - service: atlassian
    why: "GSD MCP needs an Atlassian Cloud API token to read/write Jira tickets in the SIZE project"
    env_vars:
      - name: ATLASSIAN_API_TOKEN
        source: "https://id.atlassian.com/manage-profile/security/api-tokens — Create API token, scope = Jira"
    dashboard_config:
      - task: "Confirm board 100 exists for project SIZE"
        location: "https://cranialtrading.atlassian.net/jira/software/projects/SIZE/boards/100"
must_haves:
  truths:
    - "CLAUDE.md contains an explicit reference to CON-013 (60-combo / 17-style smoke) inside the Definition-of-Done section"
    - "CLAUDE.md contains an 'Atlassian API token setup' section that names the env var, the token-generation URL, and how to verify the connection"
    - ".mcp.json registers the GSD MCP server and references the Atlassian token via ${env:ATLASSIAN_API_TOKEN} interpolation (never inlined)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Augmented agent context with DoD CON-013 + Atlassian token setup"
      contains: "CON-013"
    - path: ".mcp.json"
      provides: "Project-level MCP server registration for GSD MCP → SIZE Jira"
      contains: "ATLASSIAN_API_TOKEN"
  key_links:
    - from: ".mcp.json env block"
      to: "process env ATLASSIAN_API_TOKEN"
      via: "${env:ATLASSIAN_API_TOKEN} interpolation"
      pattern: "\\$\\{env:ATLASSIAN_API_TOKEN\\}"
    - from: "CLAUDE.md DoD section"
      to: "CON-013 60-combo / 17-style smoke"
      via: "Definition of Done note"
      pattern: "CON-013"
---

<objective>
Augment CLAUDE.md with the Definition-of-Done CON-013 reference + a concrete Atlassian-token setup section, and write `.mcp.json` so every Claude Code session in this checkout auto-connects to the GSD MCP server for the SIZE Jira project.

Purpose: Closes Phase 1 success criterion #4 (DoD note pointing to CON-013) and pre-stages Phase 1 success criterion #2 (GSD MCP reachable) — actual Jira reachability verification + epic seeding happens in Plan 04.
Output: CLAUDE.md updated, `.mcp.json` committed at repo root with env-var-interpolated token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cierre-deploy/09-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Augment CLAUDE.md with CON-013 DoD reference + Atlassian token setup section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (existing content — current sections: REGLAS INVIOLABLES, Stack, Conventions, Commands, GSD workflow, Agentic ops, What NOT to do)
    - .planning/PROJECT.md (DEC-009 / CON-013 — DoD includes WCAG AA + affected market×size combinations smoke)
    - .planning/phases/09-cierre-deploy/09-CONTEXT.md (Definition-of-Done note + Atlassian token setup duty)
  </read_first>
  <action>
    Edit `CLAUDE.md` (do NOT rewrite the whole file — preserve existing reglas inviolables, stack, conventions, commands sections).

    Add two new sections.

    1) Insert a new section titled `## Definition of Done` BEFORE the existing `## What NOT to do` section. Content:

    ```markdown
    ## Definition of Done

    Every view-touching ticket must verify, before moving to Done:

    - **WCAG AA legibility (LOCKED-001 / DEC-009)** — body ≥ 4.5:1, large text ≥ 3:1, in the styles the ticket touches.
    - **Style smoke (CON-013)** — affected market×size combinations still function. The site has 17 mutually exclusive styles (12 markets + 5 sizes); a ticket that touches a view re-verifies that the styles applicable to that view still render legibly. The 60-combination cross-product is forbidden by LOCKED-002 — smoke checks the 17, not the 60.
    - **Build green** — `pnpm type-check` and `pnpm build` both pass.
    - **Jira ticket** — moved to Done with the WCAG AA + CON-013 verification noted in the comment trail.
    ```

    2) Insert a new section titled `## Atlassian API token setup` AFTER the existing `## Agentic ops` section. Content:

    ```markdown
    ## Atlassian API token setup

    GSD MCP needs an Atlassian Cloud API token to read/write Jira tickets in the SIZE project.

    1. Generate a token at https://id.atlassian.com/manage-profile/security/api-tokens (scope: Jira). Copy the token immediately — Atlassian only shows it once.
    2. Export it in your shell environment:
       - PowerShell: `$env:ATLASSIAN_API_TOKEN = "<token>"`
       - bash/zsh: `export ATLASSIAN_API_TOKEN="<token>"`
    3. Persist it for future sessions in your shell profile (`$PROFILE` for PowerShell, `~/.bashrc` / `~/.zshrc` for POSIX shells). Never commit the token; `.mcp.json` interpolates it via `${env:ATLASSIAN_API_TOKEN}`.
    4. Verify the connection by asking Claude Code (in this checkout) to list issues in project SIZE — the GSD MCP server registered in `.mcp.json` picks up the token from the environment automatically.
    ```

    Do not modify any other content. Do not collapse or reorder existing sections.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('CLAUDE.md','utf8'); const checks=[/## Definition of Done/, /CON-013/, /## Atlassian API token setup/, /ATLASSIAN_API_TOKEN/, /id\\.atlassian\\.com\\/manage-profile\\/security\\/api-tokens/]; for(const c of checks){if(!c.test(s)){console.error('missing',c);process.exit(1)}} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '## Definition of Done' CLAUDE.md` matches exactly one line
    - `grep -F 'CON-013' CLAUDE.md` matches at least once inside the Definition of Done section
    - `grep -F '## Atlassian API token setup' CLAUDE.md` matches exactly one line
    - `grep -F 'ATLASSIAN_API_TOKEN' CLAUDE.md` matches at least once
    - `grep -F 'id.atlassian.com/manage-profile/security/api-tokens' CLAUDE.md` matches exactly once
    - The pre-existing sections `## REGLAS INVIOLABLES`, `## Stack`, `## Commands`, `## Agentic ops`, `## What NOT to do` still exist (`grep -F` each, all must match)
  </acceptance_criteria>
  <done>CLAUDE.md augmented with DoD CON-013 reference + Atlassian token setup section; pre-existing sections preserved</done>
</task>

<task type="auto">
  <name>Task 2: Write .mcp.json registering GSD MCP for SIZE Jira</name>
  <files>.mcp.json</files>
  <read_first>
    - .planning/phases/09-cierre-deploy/09-CONTEXT.md (specifics: env-var interpolation pattern `${env:ATLASSIAN_API_TOKEN}`; Jira board reference `https://cranialtrading.atlassian.net/jira/software/projects/SIZE/boards/100`)
    - CLAUDE.md (must already contain the `## Atlassian API token setup` section from Task 1 — confirm before writing)
  </read_first>
  <action>
    Create `.mcp.json` at the repo root with this exact content (per Claude Code project-level MCP config conventions: top-level `mcpServers` object keyed by server name, each server an object with `command`, `args`, `env`):

    ```json
    {
      "mcpServers": {
        "gsd": {
          "command": "npx",
          "args": ["-y", "@gsd-build/mcp@latest"],
          "env": {
            "ATLASSIAN_SITE": "cranialtrading.atlassian.net",
            "ATLASSIAN_EMAIL": "srparca@gmail.com",
            "ATLASSIAN_API_TOKEN": "${env:ATLASSIAN_API_TOKEN}",
            "JIRA_PROJECT_KEY": "SIZE",
            "JIRA_BOARD_ID": "100"
          }
        }
      }
    }
    ```

    Notes on every value:
    - `command: "npx"` + `args: ["-y", "@gsd-build/mcp@latest"]` — runs the GSD MCP server via npx without requiring a global install. If the actual GSD MCP package is published under a different name, the executor verifies via `npm view @gsd-build/mcp` (and falls back to `npm view gsd-mcp` / `npm view @gsd/mcp`) and updates the args; if none of the three resolve, abort and report — that is a researcher-level discovery, not a planner-level one. Do not invent a package name.
    - `ATLASSIAN_SITE`: hard-coded to `cranialtrading.atlassian.net` per CONTEXT.md.
    - `ATLASSIAN_EMAIL`: `srparca@gmail.com` (the Atlassian account that owns the API token).
    - `ATLASSIAN_API_TOKEN`: `${env:ATLASSIAN_API_TOKEN}` — env-var interpolation, never inline. Plan 04 verifies the token is exported before invoking MCP.
    - `JIRA_PROJECT_KEY`: `SIZE` per DEC-008.
    - `JIRA_BOARD_ID`: `100` per CONTEXT.md.

    Do NOT commit the token itself. Do NOT add any other MCP servers — this file is scoped to GSD MCP only for Phase 1.
  </action>
  <verify>
    <automated>node -e "const j=require('./.mcp.json'); const g=j.mcpServers&&j.mcpServers.gsd; if(!g)process.exit(1); if(g.env.ATLASSIAN_API_TOKEN!=='${env:ATLASSIAN_API_TOKEN}')process.exit(2); if(g.env.ATLASSIAN_SITE!=='cranialtrading.atlassian.net')process.exit(3); if(g.env.JIRA_PROJECT_KEY!=='SIZE')process.exit(4); if(g.env.JIRA_BOARD_ID!=='100')process.exit(5); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `.mcp.json` is valid JSON parseable by Node
    - `mcpServers.gsd.env.ATLASSIAN_API_TOKEN` equals literal string `${env:ATLASSIAN_API_TOKEN}` (no inlined secret)
    - `mcpServers.gsd.env.ATLASSIAN_SITE` equals `cranialtrading.atlassian.net`
    - `mcpServers.gsd.env.JIRA_PROJECT_KEY` equals `SIZE`
    - `mcpServers.gsd.env.JIRA_BOARD_ID` equals `100`
    - `grep -F '${env:ATLASSIAN_API_TOKEN}' .mcp.json` matches; no raw token-shaped string (`grep -E 'ATATT[0-9A-Za-z]{20,}' .mcp.json` returns no match)
  </acceptance_criteria>
  <done>.mcp.json registers GSD MCP for SIZE Jira; secret is env-var-interpolated; Plan 04 will verify reachability</done>
</task>

</tasks>

<verification>
- CLAUDE.md contains `## Definition of Done` with CON-013 reference
- CLAUDE.md contains `## Atlassian API token setup` with the env var name and token URL
- `.mcp.json` parses as JSON; the GSD MCP server's `ATLASSIAN_API_TOKEN` is the interpolation literal, not a real token
- No raw Atlassian token strings appear in the repo (`grep -rE 'ATATT[0-9A-Za-z]{20,}' .` returns no match)
</verification>

<success_criteria>
Phase 1 success criterion #4 (DoD note in CLAUDE.md referencing CON-013) is closed. The wiring half of success criterion #2 (GSD MCP reachable from this checkout) is staged — Plan 04 verifies reachability and seeds the 9 Jira epics.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cierre-deploy/09-02-SUMMARY.md`.
</output>
