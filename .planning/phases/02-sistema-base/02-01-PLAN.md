---
phase: 02-sistema-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/markets.css
  - src/styles/main.css
  - index.html
autonomous: true
requirements:
  - REQ-style-persistence
must_haves:
  truths:
    - "Toggling between any size and any market restyles the body in ~600ms (not 500ms)"
    - "Each of the 12 markets has a working token block that overrides --bg, --ink, --muted, --line, --line-strong, --accent, and --font-display"
    - "The 12 per-market display fonts load from Google Fonts on initial page load"
    - "Every market token block satisfies WCAG AA (≥ 4.5:1 contrast for body text on --bg) — verified by an automated luminance check in Task 1"
  artifacts:
    - path: "src/styles/markets.css"
      provides: "12 market token blocks (cpg, banca, retail, automotriz, salud, bebidas, inmobiliario, educacion, turismo, tecnologia, moda, fintech)"
      contains: "html.market-cpg"
    - path: "src/styles/main.css"
      provides: "@import of markets.css and 600ms body transition"
      contains: "@import \"./markets.css\""
    - path: "index.html"
      provides: "Google Fonts link including the 12 market display fonts"
      contains: "Bodoni+Moda"
  key_links:
    - from: "src/styles/main.css"
      to: "src/styles/markets.css"
      via: "@import"
      pattern: "@import\\s+[\"']\\.\\/markets\\.css[\"']"
    - from: "html.has-market.market-{id}"
      to: "per-market token values"
      via: "CSS custom properties cascade"
      pattern: "html\\.market-(cpg|banca|retail|automotriz|salud|bebidas|inmobiliario|educacion|turismo|tecnologia|moda|fintech)"
---

<objective>
This plan delivers the 12 market CSS token blocks and the 600ms transition timing alignment. Plan 02-03 hardens routing in `src/App.vue`; the existing `watchEffect` in `src/App.vue` already handles `level-{x}` / `has-market` / `market-{id}` class swaps (verified by reading the file). The verification scenario in this plan uses manual `document.documentElement.classList.add(...)` to test the tokens in isolation, so this plan can land independently in Wave 1 alongside 02-02 and 02-03.

Purpose: Phase 2 ships the **infrastructure** for 12 markets + 5 sizes as independent token sets. The actual full per-market visual treatment ships in Phase 4 — this plan only delivers the token blocks (palette/accent/radius/font-display). Body stays Geist on every market per CONTEXT.md D-04 (per-market typography v1).

Output: `src/styles/markets.css` exists with 12 blocks; `src/styles/main.css` imports it and uses 600ms transition; `index.html` references the 12 display fonts. No JS change required (App.vue's existing watchEffect already adds `market-{id}` classes to `<html>`).

**Verify-command convention:** All `<verify>` commands assume Bash semantics (the Bash tool is available alongside PowerShell on this dev machine). On PowerShell-only environments, prefix with `bash -lc "..."` to avoid quoting hazards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sistema-base/02-CONTEXT.md
@CLAUDE.md
@src/styles/main.css
@index.html
@src/App.vue

<interfaces>
<!-- Pinia store types and watcher conventions the executor must respect. -->

From src/stores/style.ts (already in repo, do not modify here):
```typescript
export type MarketId =
  | 'cpg' | 'banca' | 'retail' | 'automotriz' | 'salud' | 'bebidas'
  | 'inmobiliario' | 'educacion' | 'turismo' | 'tecnologia' | 'moda' | 'fintech'
```

From src/App.vue (already wires `market-{id}` to <html>):
```typescript
// On store change, applies: html.classList.add('level-m', 'has-market', `market-${active.value}`)
```

The 5 level token blocks already exist in main.css at lines 108–170 (level-m, level-s, level-l, level-xs, level-xl). Markets follow the same shape but on `html.market-{id}` selectors (NOT `html.level-*`), and they coexist with `level-m` because markets always render on the M baseline (Phase 4 spec).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/styles/markets.css with 12 market token blocks (with WCAG-AA verifier)</name>
  <read_first>
    - src/styles/markets.css (target — confirm does not exist yet; create new)
    - src/styles/main.css (lines 108–170 — copy the level-* block shape)
    - .planning/phases/02-sistema-base/02-CONTEXT.md `<specifics>` block (lines 186–199 — display font assignments per market)
    - ExistingData/SIZE-design-brief-v4.md §5 (master palette intent per market — read before picking exact colors)
  </read_first>
  <files>src/styles/markets.css</files>
  <action>
Create `src/styles/markets.css` with 12 blocks following the exact shape of the existing `html.level-m` block in `main.css`. Each block selector is `html.market-{id}` and overrides: `--bg`, `--ink`, `--muted`, `--line`, `--line-strong`, `--accent`, `--accent-2` (optional), `--radius`, `--font-display`. Body and mono fonts are NOT overridden (they stay Geist / Geist Mono per D-04 in CONTEXT.md).

WCAG AA constraint (LOCKED-001): `--ink` on `--bg` MUST achieve ≥ 4.5:1 contrast for body text in every block. Pick palettes that satisfy this. When in doubt, default `--ink` to `#0a0a0a` on light backgrounds and `#f5f5f5` on dark backgrounds. The verify step below computes the ratio for each block and fails if any block is below 4.5:1.

The 12 markets, in order, with their display fonts (per CONTEXT.md `<specifics>`, with `Source Serif Pro` updated to `Source Serif 4` — the current Google Fonts name):

1. `html.market-cpg` — Consumo masivo. Bright/optimistic palette (cream/orange family). `--font-display: "Poppins", sans-serif;`
2. `html.market-banca` — Banca. Conservative blue/navy + cream. `--font-display: "Source Serif 4", serif;`
3. `html.market-retail` — Retail. High-contrast, energetic (red/black). `--font-display: "Bebas Neue", sans-serif;`
4. `html.market-automotriz` — Automotriz. Industrial dark + electric accent. `--font-display: "Saira", sans-serif;`
5. `html.market-salud` — Salud. Soft mint/white, calm. `--font-display: "Inter", sans-serif;` `--radius: 12px;`
6. `html.market-bebidas` — Bebidas. Warm amber/burgundy. `--font-display: "Cormorant Garamond", serif;`
7. `html.market-inmobiliario` — Inmobiliario. Stone/charcoal + gold accent. `--font-display: "Playfair Display", serif;`
8. `html.market-educacion` — Educación. Friendly cream + accent blue. `--font-display: "Inter", sans-serif;` `--radius: 8px;`
9. `html.market-turismo` — Turismo. Warm sand + ocean teal. `--font-display: "Cormorant Garamond", serif;`
10. `html.market-tecnologia` — Tecnología. Mono dark + neon green. `--font-display: "JetBrains Mono", monospace;`
11. `html.market-moda` — Moda. Stark black/white editorial. `--font-display: "Bodoni Moda", serif;`
12. `html.market-fintech` — Fintech. Deep purple/navy + lime. `--font-display: "Sora", sans-serif;`

File header comment at top:
```css
/*
  SIZE — Markets token blocks (Phase 2)
  12 markets, each with palette + accent + radius + display font.
  Body stays Geist (D-04 in 02-CONTEXT.md). Self-hosting deferred to polish pass (DEC-007).
  Phase 4 will layer full visual treatments on top of these tokens.
*/
```

Each block must have all 8 token properties listed (the 7 colors + radius + font-display). Format each block exactly like the `level-m` block in `main.css` for consistency. Use `#rrggbb` hex literals for `--bg` and `--ink` (the verifier parses these directly).
  </action>
  <verify>
    <automated>bash -lc "node -e \"const fs=require('fs');const c=fs.readFileSync('src/styles/markets.css','utf8');const ids=['cpg','banca','retail','automotriz','salud','bebidas','inmobiliario','educacion','turismo','tecnologia','moda','fintech'];for(const id of ids){if(!c.includes('html.market-'+id)){throw new Error('Missing block: '+id)}};const fonts=['Poppins','Source Serif 4','Bebas Neue','Saira','Inter','Cormorant Garamond','Playfair Display','JetBrains Mono','Bodoni Moda','Sora'];for(const f of fonts){if(!c.includes(f)){throw new Error('Missing font: '+f)}};function lum(hex){const h=hex.replace('#','');const r=parseInt(h.slice(0,2),16)/255;const g=parseInt(h.slice(2,4),16)/255;const b=parseInt(h.slice(4,6),16)/255;const f=v=>v<=0.03928?v/12.92:Math.pow((v+0.055)/1.055,2.4);return 0.2126*f(r)+0.7152*f(g)+0.0722*f(b)};function ratio(a,b){const la=lum(a),lb=lum(b);const hi=Math.max(la,lb),lo=Math.min(la,lb);return (hi+0.05)/(lo+0.05)};const blockRe=/html\\.market-([a-z]+)\\s*\\{([^}]+)\\}/g;let m,checked=0,failures=[];while((m=blockRe.exec(c))){const id=m[1];const body=m[2];const bg=(body.match(/--bg:\\s*(#[0-9a-fA-F]{6})/)||[])[1];const ink=(body.match(/--ink:\\s*(#[0-9a-fA-F]{6})/)||[])[1];if(!bg||!ink){failures.push(id+': missing #rrggbb --bg or --ink');continue};const r=ratio(bg,ink);if(r<4.5){failures.push(id+': contrast '+r.toFixed(2)+':1 below 4.5:1 ('+bg+' vs '+ink+')')};checked++};if(checked<12)throw new Error('Only '+checked+' blocks parsed for contrast (expected 12)');if(failures.length)throw new Error('WCAG AA failures:\\n'+failures.join('\\n'));console.log('OK — 12 blocks, all ≥ 4.5:1')\""</automated>
  </verify>
  <acceptance_criteria>
    - File `src/styles/markets.css` exists.
    - Contains 12 selectors: `html.market-cpg`, `html.market-banca`, `html.market-retail`, `html.market-automotriz`, `html.market-salud`, `html.market-bebidas`, `html.market-inmobiliario`, `html.market-educacion`, `html.market-turismo`, `html.market-tecnologia`, `html.market-moda`, `html.market-fintech` (exact strings, grep-checkable).
    - Each block declares `--bg`, `--ink`, `--muted`, `--line`, `--line-strong`, `--accent`, `--radius`, `--font-display` at minimum, with `--bg` and `--ink` as `#rrggbb` hex literals.
    - The 10 unique display fonts referenced: `Poppins`, `Source Serif 4`, `Bebas Neue`, `Saira`, `Inter`, `Cormorant Garamond`, `Playfair Display`, `JetBrains Mono`, `Bodoni Moda`, `Sora` (Inter and Cormorant each appear twice).
    - No body or mono font is overridden (no `--font-body` or `--font-mono` lines anywhere in the file).
    - WCAG AA: every block's `--ink` on `--bg` ratio ≥ 4.5:1 (enforced by verify command).
    - Verify command above exits 0.
  </acceptance_criteria>
  <done>The 12 market token blocks live in src/styles/markets.css. Body fonts not overridden. All 12 satisfy WCAG AA. Verification script prints OK.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire markets.css into main.css and align body transition to 600ms</name>
  <read_first>
    - src/styles/main.css (lines 36–44 — body transition declaration; lines 1–22 — top of file where @import goes)
    - .planning/phases/02-sistema-base/02-CONTEXT.md (carryovers section — "~600ms transitions on style swaps (CON-008)")
  </read_first>
  <files>src/styles/main.css</files>
  <action>
Two edits in `src/styles/main.css`:

1. **Add `@import "./markets.css";`** immediately after the existing `@import "tailwindcss";` line at the top of the file (around line 1–2). This brings the 12 market token blocks into the global cascade.

2. **Change body transition from 500ms to 600ms** in the `body { ... }` rule (currently around lines 36–44). The existing block has:
```css
body {
  ...
  transition:
    background 500ms ease,
    color 500ms ease;
}
```

Change BOTH `500ms` to `600ms` so it reads:
```css
body {
  ...
  transition:
    background 600ms ease,
    color 600ms ease;
}
```

Do NOT touch the `@keyframes reconfigure` block (already 600ms). Do NOT touch the `prefers-reduced-motion` block at the bottom (already correct).
  </action>
  <verify>
    <automated>bash -lc "node -e \"const c=require('fs').readFileSync('src/styles/main.css','utf8');if(!/@import\\s+[\\\"']\\.\\/markets\\.css[\\\"']/.test(c))throw new Error('markets.css not imported');if(!/background\\s+600ms\\s+ease/.test(c))throw new Error('background transition not 600ms');if(!/color\\s+600ms\\s+ease/.test(c))throw new Error('color transition not 600ms');if(/background\\s+500ms/.test(c)||/color\\s+500ms/.test(c))throw new Error('500ms transition still present');console.log('OK')\""</automated>
  </verify>
  <acceptance_criteria>
    - main.css contains exactly one `@import "./markets.css"` (or `@import './markets.css'`) line.
    - main.css body block uses `background 600ms ease, color 600ms ease`.
    - No `500ms` substring remains in the body transition (other 500ms references elsewhere in file are acceptable; this checks the body block specifically).
    - Verify command exits 0.
  </acceptance_criteria>
  <done>main.css imports markets.css and uses 600ms body transition.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend index.html Google Fonts link with the 12 market display fonts</name>
  <read_first>
    - index.html (line 11 — existing fonts link with Archivo Black, Geist, Geist Mono, Instrument Serif)
    - .planning/phases/02-sistema-base/02-CONTEXT.md `<specifics>` (the 10 unique display font names)
  </read_first>
  <files>index.html</files>
  <action>
Replace the existing Google Fonts `<link href="https://fonts.googleapis.com/css2?...">` line (line 11) with a single new `<link>` that includes ALL of the existing fonts PLUS the 10 new market display fonts.

Existing fonts (must keep): `Archivo Black`, `Geist:wght@300;400;500;600;700;800;900`, `Geist Mono:wght@400;500`, `Instrument Serif:ital@0;1`.

New fonts to add (10 unique — Inter and Cormorant Garamond each cover two markets). Use these exact family strings:
- `Bebas Neue`
- `Bodoni Moda:ital,wght@0,400..900;1,400..900` (variable, italic + roman)
- `Cormorant Garamond:ital,wght@0,400;0,600;1,400`
- `Inter:wght@400;500;600;700`
- `JetBrains Mono:wght@400;700`
- `Playfair Display:ital,wght@0,400;0,700;1,400`
- `Poppins:wght@400;600;700`
- `Saira:wght@400;600;800`
- `Sora:wght@400;600;800`
- `Source Serif 4:ital,wght@0,400;0,600;1,400` (this is the current Google Fonts name — formerly Source Serif Pro; the markets.css banca block uses the matching `"Source Serif 4"` family)

Concatenate them all into one `family=...&family=...` link with `display=swap` at the end. Place fonts in alphabetical order for readability.

The final tag must be ONE single `<link>` element (not multiple) so we don't multiply HTTP requests, and must end with `display=swap`.

Keep `<link rel="preconnect" href="https://fonts.googleapis.com" />` and `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />` lines unchanged.
  </action>
  <verify>
    <automated>bash -lc "node -e \"const c=require('fs').readFileSync('index.html','utf8');const required=['Archivo+Black','Geist','Geist+Mono','Instrument+Serif','Bebas+Neue','Bodoni+Moda','Cormorant+Garamond','Inter','JetBrains+Mono','Playfair+Display','Poppins','Saira','Sora','Source+Serif+4'];for(const f of required){if(!c.includes(f))throw new Error('Missing font in index.html: '+f)};if(/Source\\+Serif\\+Pro/.test(c))throw new Error('Stale Source Serif Pro reference — use Source Serif 4');if(!/display=swap/.test(c))throw new Error('display=swap missing');const linkMatches=c.match(/<link[^>]*fonts\\.googleapis\\.com[^>]*>/g)||[];if(linkMatches.length!==1)throw new Error('Expected 1 fonts.googleapis.com link, got '+linkMatches.length);console.log('OK')\""</automated>
  </verify>
  <acceptance_criteria>
    - index.html has exactly one `<link>` tag pointing to `fonts.googleapis.com/css2`.
    - That link references all 14 font families: 4 existing (Archivo Black, Geist, Geist Mono, Instrument Serif) + 10 new (Bebas Neue, Bodoni Moda, Cormorant Garamond, Inter, JetBrains Mono, Playfair Display, Poppins, Saira, Sora, Source Serif 4).
    - The string `Source+Serif+Pro` does NOT appear anywhere (replaced by `Source+Serif+4`).
    - `display=swap` is present.
    - The two `preconnect` links remain unchanged.
    - Verify command exits 0.
  </acceptance_criteria>
  <done>index.html loads all market display fonts in one Google Fonts request, using the modern `Source Serif 4` family name.</done>
</task>

</tasks>

<verification>
After all 3 tasks:

1. `pnpm build` (or `pnpm type-check`) completes with no CSS or HTML errors.
2. `bash -lc 'grep -c "html.market-" src/styles/markets.css'` returns ≥ 12.
3. Open the dev server (`pnpm dev`), open DevTools, run `document.documentElement.classList.add('has-market', 'market-banca')` in console — body background and ink should change with a smooth 600ms transition.
4. In DevTools Network tab, confirm the Google Fonts CSS request returns 200 and the response body contains `@font-face` blocks for at least Bebas Neue, Poppins, and Bodoni Moda.
</verification>

<success_criteria>
- `src/styles/markets.css` exists with 12 `html.market-{id}` blocks (one per market).
- Each block defines palette + accent + radius + display font; none override `--font-body` or `--font-mono`.
- All 12 blocks pass the WCAG-AA contrast verifier (≥ 4.5:1 on `--ink` over `--bg`).
- `src/styles/main.css` imports `./markets.css` and uses 600ms body transitions.
- `index.html` loads all 10 unique market display fonts via Google Fonts, using `Source Serif 4` (not `Source Serif Pro`).
- `pnpm build` succeeds; existing app still works at level-m default.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sistema-base/02-01-SUMMARY.md` summarizing:
- Files created/modified.
- Final list of 12 markets with their assigned palette accents and display fonts.
- Confirmation that `Source Serif 4` was used for `market-banca` (and the matching string is in `index.html`).
- Per-market WCAG-AA contrast ratios produced by the verifier (so Phase 4 can validate without re-deriving).
</output>

<deferred>
- Per-market large-text contrast (≥ 3:1 for ≥ 18pt or 14pt bold text) is not enforced by the Task 1 verifier — body-text 4.5:1 is the floor here. Phase 4 may tighten when full per-market visual treatments land and large-text usages are concrete.
- Self-hosted variable fonts (DEC-007) — still deferred per CONTEXT.md.
</deferred>
</content>
</invoke>