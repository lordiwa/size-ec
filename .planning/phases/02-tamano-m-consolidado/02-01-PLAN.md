---
phase: 02-tamano-m-consolidado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ChangeStyleControl.vue
  - src/views/ServiciosView.vue
  - src/views/QuienesSomosView.vue
  - src/views/ContactoView.vue
autonomous: true
requirements: [REQ-services-catalog, REQ-contact-view-layout]
must_haves:
  truths:
    - "Servicios in no-market case ends with a centered '.bright-cta' that links to /contacto"
    - "Servicios, Quiénes somos, and Contacto each expose a 'Cambiar estilo' control that reopens the intensity gate without clearing the current level"
    - "The reopened gate retains the previously highlighted level tick (no jump back to M)"
    - "Picking a level inside the reopened gate fires the Reconfigurando overlay and applies the new theme"
  artifacts:
    - path: "src/components/ChangeStyleControl.vue"
      provides: "Reusable mono-uppercase button used across Servicios / Quiénes somos / Contacto to reopen the gate"
      contains: "Cambiar estilo"
  key_links:
    - from: "ChangeStyleControl click"
      to: "useStyleStore().reopenGate()"
      via: "Pinia action that flips levelChosen=false without resetting level"
      pattern: "levelChosen.value = false"
    - from: "Servicios no-market grid foot"
      to: "/contacto"
      via: "RouterLink with bright-cta styling"
      pattern: "bright-cta"
---

<objective>
Add the two interaction surfaces the Phase 1 prototype-port deferred:

1. A "Cambiar estilo" control on Servicios, Quiénes somos, and Contacto so a user past the gate can re-trigger it without navigating Home.
2. A bright CTA at the bottom of the no-market Servicios grid pointing to /contacto, closing the funnel.

Both are direct ROADMAP §Phase 2 success criterion #2 obligations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tamano-m-consolidado/02-CONTEXT.md
@src/views/ServiciosView.vue
@src/views/QuienesSomosView.vue
@src/views/ContactoView.vue
@src/components/IntensityChooser.vue
@src/stores/style.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reopenGate() action to the style store</name>
  <files>src/stores/style.ts</files>
  <read_first>
    - src/stores/style.ts (current state — no reopenGate yet)
    - .planning/phases/02-tamano-m-consolidado/02-CONTEXT.md (D-01: reopen does NOT reset level)
  </read_first>
  <action>
    Add a `reopenGate` action to the Pinia store. It must set `levelChosen.value = false` and leave `level.value` and `marketId.value` untouched. Document inline that this is intentionally not a reset — it lets the user re-pick intensity while remembering their last choice as the default-highlighted tick in the modal.

    Export the action from the store's return tuple alongside the existing `setLevel`, `setMarketId`, `resetAll`.

    Do NOT touch any other store behavior. Do NOT re-introduce localStorage.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/stores/style.ts','utf8'); if(!/function reopenGate\\b/.test(s))process.exit(1); if(!/levelChosen\\.value\\s*=\\s*false/.test(s))process.exit(2); if(!/return\\s*{[^}]*reopenGate/.test(s))process.exit(3); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `reopenGate` is defined in `src/stores/style.ts` and returned from `defineStore`
    - The body sets `levelChosen.value = false` and does NOT mutate `level` or `marketId`
    - Type-check passes
  </acceptance_criteria>
  <done>Store gains a non-destructive gate-reopener; level memory survives the reopen.</done>
</task>

<task type="auto">
  <name>Task 2: Author ChangeStyleControl reusable component</name>
  <files>src/components/ChangeStyleControl.vue</files>
  <read_first>
    - src/components/IntensityChooser.vue (token/spacing conventions)
    - src/views/ServiciosView.vue (existing market-mode "← Cambiar categoría" button — reuse its visual weight as a sibling)
  </read_first>
  <action>
    Create `src/components/ChangeStyleControl.vue` with `<script setup lang="ts">`. The component is a single button:

    - Class: mono-uppercase pill, font-size 11px, padding 8px 14px, border 1px solid var(--line-strong), border-radius 999px, background transparent, color var(--muted), hover color var(--ink).
    - Label: "Cambiar estilo".
    - aria-label: "Cambiar el estilo de la página".
    - Click: calls `useStyleStore().reopenGate()`.
    - Emits no events; the gate-modal mount in App.vue picks up the state change automatically.

    Scoped CSS only. The component must work on a dark M background (the only Phase 2 target) and on any market background (since `var(--muted)` resolves correctly when has-market is set). No level-specific overrides — Phases 4-6 add those when needed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='src/components/ChangeStyleControl.vue'; if(!fs.existsSync(p))process.exit(1); const s=fs.readFileSync(p,'utf8'); const checks=[/&lt;script setup lang=\"ts\"&gt;/, /useStyleStore/, /reopenGate\\(\\)/, /Cambiar estilo/, /aria-label/]; for(const c of checks){if(!c.test(s)){console.error('missing',c);process.exit(1)}} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `<script setup lang="ts">` block
    - Imports `useStyleStore`
    - Click handler calls `reopenGate()` exactly once
    - Visible label is "Cambiar estilo"
    - aria-label set
  </acceptance_criteria>
  <done>Reusable single-button component ready for mounting in three views.</done>
</task>

<task type="auto">
  <name>Task 3: Mount Cambiar estilo + Hablemos CTA on ServiciosView (no-market)</name>
  <files>src/views/ServiciosView.vue</files>
  <read_first>
    - src/views/ServiciosView.vue (no-market branch + market branch)
    - .planning/phases/02-tamano-m-consolidado/02-CONTEXT.md (D-02: CTA copy "Hablemos →" + target /contacto + only in no-market case)
    - src/styles/main.css (.bright-cta atom)
  </read_first>
  <action>
    In the no-market branch (the `<template v-if="!style.market">` block):

    1. Inside the `<section class="srv-head">`, after the existing `.srv-eyebrow`, render a horizontal row with the eyebrow text already there on the left and a `<ChangeStyleControl />` mount on the right. Use a flex container; on mobile (≤720px) it stacks. Do NOT relocate the existing eyebrow to a different parent — wrap it in a flex row only inside this branch.

    2. After the closing `</div>` of `.srv-grid` and before `.srv-foot`, add a centered `<RouterLink :to="{ name: 'contacto' }" class="bright-cta srv-cta-contact">Hablemos <span aria-hidden="true">→</span></RouterLink>`. The `srv-cta-contact` scoped style centers it horizontally with `margin: 4vh auto 0; display: inline-flex;` and parent gets `text-align: center` for the surrounding section, OR wrap the link in a `<div class="srv-cta-row">` styled as flex/center.

    Do NOT modify the market-mode branch (`<section v-else class="srv-mkt">`) — it already has "← Cambiar categoría". Do NOT change the existing card grid markup.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/views/ServiciosView.vue','utf8'); const checks=[/import ChangeStyleControl/, /&lt;ChangeStyleControl \\/&gt;/, /Hablemos/, /name: 'contacto'/]; for(const c of checks){if(!c.test(s)){console.error('missing',c);process.exit(1)}} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - ServiciosView imports `ChangeStyleControl` from `@/components/ChangeStyleControl.vue`
    - The control is rendered in the no-market head row, not the market branch
    - "Hablemos →" RouterLink rendered after `.srv-grid` with `name: 'contacto'`
    - `pnpm type-check` passes
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Servicios M no-market case is feature-complete per ROADMAP §Phase 2 SC #2.</done>
</task>

<task type="auto">
  <name>Task 4: Mount Cambiar estilo on QuienesSomosView and ContactoView</name>
  <files>src/views/QuienesSomosView.vue, src/views/ContactoView.vue</files>
  <read_first>
    - src/views/QuienesSomosView.vue (current head section)
    - src/views/ContactoView.vue (current head section)
    - src/components/ChangeStyleControl.vue (the new reusable button)
  </read_first>
  <action>
    In both QuienesSomosView and ContactoView, add a `<ChangeStyleControl />` mount inside the existing `<section class="qn-head">` / `<section class="ct-head">` block. Use the same flex-row pattern as ServiciosView so the eyebrow stays centered and the control sits at the right edge of the head row (or stacks under it on mobile).

    No other markup changes. The control's reopenGate side-effect is what powers the behavior; the gate-modal is rendered from App.vue.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const p of ['src/views/QuienesSomosView.vue','src/views/ContactoView.vue']){const s=fs.readFileSync(p,'utf8'); if(!/import ChangeStyleControl/.test(s)){console.error('missing import',p);process.exit(1)} if(!/&lt;ChangeStyleControl \\/&gt;/.test(s)){console.error('missing mount',p);process.exit(1)}} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - QuienesSomosView and ContactoView both import and render `ChangeStyleControl`
    - Each mount lives inside the existing head section
    - `pnpm type-check` and `pnpm build` pass
    - Visual smoke: clicking the control on any of the three views opens the IntensityChooser modal with the previously selected tick highlighted
  </acceptance_criteria>
  <done>All three protected views expose the gate-reopener.</done>
</task>

</tasks>

<verification>
- `pnpm type-check` exits 0
- `pnpm build` exits 0
- ChangeStyleControl mounted in 3 views; click reopens gate without resetting level
- Servicios no-market grid ends with a "Hablemos →" CTA pointing to /contacto
- Manual: pick L from gate → land on /servicios → click "Cambiar estilo" → modal opens with L still active → pick M → Reconfigurando plays → land back on /servicios styled as M
</verification>

<success_criteria>
ROADMAP §Phase 2 success criterion #2 fully satisfied: Servicios renders all 11 services in canonical order, ends with a CTA to Contacto, and exposes a "Cambiar estilo" control. The same control lives on Quiénes somos and Contacto for symmetry.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tamano-m-consolidado/02-01-SUMMARY.md`.
</output>
